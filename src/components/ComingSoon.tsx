export default function ComingSoon() {
  return (
    <main className="flex min-h-dvh flex-col items-center justify-center px-8 text-center">
      <h1 className="font-logo text-[clamp(1.8rem,5vw,2.8rem)] tracking-[0.04em] text-[var(--color-gold)]">
        MãosFalam
      </h1>
      <p className="mt-8 max-w-[28ch] font-cormorant text-[clamp(1.1rem,3vw,1.4rem)] leading-relaxed text-[var(--color-bone-dim)]">
        Suas mãos já sabem. Você ainda não. Em breve.
      </p>
    </main>
  );
}
