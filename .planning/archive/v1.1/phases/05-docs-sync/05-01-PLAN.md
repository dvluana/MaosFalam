---
phase: 05-docs-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture.md
  - CLAUDE.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02

must_haves:
  truths:
    - "architecture.md nao menciona share_token, expires_at de credit_packs, NextAuth, R2, ou Claude Vision"
    - "architecture.md reflete fluxo unico com is_self flag, ReadingContext type, CreditGate, Prisma 7, Neon branches, e Vercel staging"
    - "CLAUDE.md lista Clerk (sem NextAuth), GPT-4o (sem Claude Vision), sem mencao a R2, e estrutura de pastas bate com src/ real"
  artifacts:
    - path: "docs/architecture.md"
      provides: "Referencia arquitetural precisa do codigo v1.1"
    - path: "CLAUDE.md"
      provides: "Instrucoes de projeto alinhadas com stack e estrutura atuais"
  key_links:
    - from: "docs/architecture.md"
      to: "prisma/schema.prisma"
      via: "Schema do banco refletido no doc sem share_token/expires_at"
    - from: "CLAUDE.md"
      to: "src/"
      via: "Estrutura de pastas listada no doc bate com ls src/"
---

<objective>
Atualizar docs/architecture.md e CLAUDE.md para refletir com precisao o codigo que existe hoje, removendo referencias a sistemas descartados e adicionando decisoes da v1.1.

Purpose: Docs desatualizados geram confusao em sessoes futuras. Claude le architecture.md antes de mexer no backend — se o doc diz "NextAuth" ou "share_token", Claude vai introduzir bugs.
Output: architecture.md e CLAUDE.md precisos, sem referencias a share_token, expires_at de creditos, NextAuth, R2, ou Claude Vision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar docs/architecture.md</name>
  <files>docs/architecture.md</files>

<read_first> - docs/architecture.md (arquivo atual completo) - prisma/schema.prisma (schema real, source of truth do banco) - src/proxy.ts (auth middleware real — clerkMiddleware, nao NextAuth) - src/types/reading-context.ts (ReadingContext type real) - src/server/lib/select-blocks.ts (motor de leitura — verificar assinatura) - src/app/api/reading/capture/route.ts (fluxo captura — verificar o que usa)
</read_first>

  <action>
Reescrever docs/architecture.md removendo todos os sistemas descartados e refletindo as decisoes da v1.1. Manter a estrutura de secoes (numeradas 1-16). Aplicar cada mudanca abaixo:

**Secao 1 (Stack):**

- Remover linha "Claude Vision" se existir
- Confirmar "GPT-4o (OpenAI API)" como unica opcao de IA de visao
- Adicionar linha: Prisma 7 com config em prisma.config.ts (nao schema.prisma)
- Adicionar linha: Neon branches — main (prod) + develop (dev)
- Adicionar linha: Vercel — staging.maosfalam.com mapeia pra develop branch

**Secao 2 (Estrutura de Pastas):**

- Remover `features/` se existir (nao existe em src/ real)
- Confirmar que `src/proxy.ts` aparece (e o auth middleware real, nao middleware.ts)
- Adicionar `src/lib/reading-context.ts` (helpers de sessionStorage pra ReadingContext)
- Remover qualquer referencia a `server/api/` separado (as routes ficam em `app/api/`)
- Remover referencias a `email-client.ts` se nao existe em `src/lib/` (nao existe)

**Secao 3 (Funil):**

- Substituir "fluxo separado pra outra pessoa" pelo fluxo unico com is_self flag:
  - /ler/nome coleta target_name + target_gender + dominant_hand + is_self via ReadingContext
  - Visitante: coleta nome + email + genero + dominancia + opt-in LGPD
  - Logada: mostra "Pra mim" / "Pra outra pessoa" na mesma tela
  - CreditGate aparece para logada na segunda leitura ou mais
  - Sem saldo → redirect /creditos antes da camera
- Substituir "Planeta dominante" por "Monte dominante" em qualquer ocorrencia

**Secao 4 (Estrutura do Relatorio):**

- Confirmar ordem v2: Prologo > Cap01 Coracao > PAYWALL > Cap02 Cabeca > Cap03 Vida > Cap04 Venus+Cinturao > Cap05 Montes > Cap06 Destino > Cap07 Cruzamentos > Cap08 Compatibilidade > Cap09 Sinais Raros > Epilogo
- Remover qualquer referencia a ordem antiga

**Secao 5 (Schema do Banco):**

- Remover share_token e share_expires_at do modelo readings
- Remover expires_at de credit_packs (creditos nao expiram)
- O schema real (prisma/schema.prisma) e a source of truth — copiar os modelos reais:
  - Lead: id, name, email, gender, sessionId, clerkUserId, emailOptIn, source, converted, createdAt
  - UserProfile: clerkUserId (PK), leadId, cpf, phone, abacatepayCustomerId, createdAt
  - Reading: id, leadId, clerkUserId, sessionId, targetName, targetGender, isSelf, attributes, report, tier, confidence, isActive, createdAt
  - CreditPack: id, clerkUserId, paymentId, packType, total, remaining, createdAt (SEM expires_at)
  - Payment: id, clerkUserId, readingId, abacatepayBillingId, packType, amountCents, method, status, paidAt, createdAt, updatedAt

**Secao 6 (Motor de Leitura):**

- Manter como esta (zero I/O, <1ms, `selectBlocks(attributes, name, gender)`)

**Secao 7 (MediaPipe):**

- Adicionar referencia ao Hand Landmarker real (@mediapipe/tasks-vision)
- Adicionar: `dominant_hand` e perguntado antes da camera e enviado no ReadingContext
- Adicionar: camera frontal espelhada com captureFrame un-mirroring a imagem antes de enviar pro GPT-4o
- Adicionar: auto-captura apos 1.5s de mao estavel e valida (nao timer mock)
- Adicionar: `src/lib/mediapipe.ts` como wrapper de inicializacao do Hand Landmarker
- Atualizar fluxo da camera para: getUserMedia → CameraViewport (video element dentro do componente) → Hand Landmarker processa frames → validacao → auto-captura base64 JPEG

**Secao 8 (AbacatePay):**

- Manter como esta (ainda e o plano, nao implementado em v1.1)
- Adicionar nota: "Implementacao adiada para milestone v2. Em v1.1, /api/credits/purchase e /api/webhook/abacatepay existem como stubs."

**Secao 9 (Auth - Clerk):**

- Substituir qualquer referencia a "middleware.ts" por "src/proxy.ts"
- Adicionar: "Clerk v7 — auth() e async, import de @clerk/nextjs/server"
- Adicionar: conta criada no primeiro pagamento ou ao salvar leitura

**Secao 10 (Email):**

- Adicionar nota: "Resend nao implementado em v1.1. Adiado para milestone futura apos dominio verificado."

**Secao 11 (API Routes):**

- Verificar se lista esta correta (sem rotas que nao existem)

**Secao 14 (Seguranca):**

- Substituir "Map em memoria" e "@upstash/ratelimit" pela situacao real: rate limiting implementado como Map in-memory (SCALE-01 adiado para v2)
- Remover referencias a R2/Cloudflare
- Confirmar: foto nunca armazenada, CPF nunca logado

**Secao 16 (Setup Local):**

- Adicionar: `cp .env.example .env.local`
- Verificar se os comandos batem com package.json scripts reais
- Adicionar Neon branches: main (prod) / develop (dev)

**Nota sobre STATUS:**

- Mudar o header "STATUS: PLANO. Backend ainda não implementado." para "STATUS: v1.1 implementado. Pagamentos (AbacatePay) e email (Resend) adiados para v2."
  </action>

    <verify>
      <automated>grep -c "share_token\|expires_at\|NextAuth\|next-auth\|R2/Cloudflare\|Claude Vision" docs/architecture.md && echo "COUNT ABOVE MUST BE 0"</automated>
    </verify>

    <done>
      - architecture.md nao tem share_token, expires_at, NextAuth, R2, ou Claude Vision
      - Schema do banco reflete os modelos reais do prisma/schema.prisma
      - Fluxo da camera descreve MediaPipe Hand Landmarker real com auto-captura
      - Fluxo /ler/nome descreve fluxo unico com is_self flag e CreditGate
      - STATUS header reflete que v1.1 esta implementado
    </done>
  </task>

<task type="auto">
  <name>Task 2: Atualizar CLAUDE.md</name>
  <files>CLAUDE.md</files>

<read_first> - CLAUDE.md (arquivo atual completo) - package.json (dependencias reais — verificar stack listado) - src/ (estrutura real de pastas)
</read_first>

  <action>
Editar CLAUDE.md para alinhar com o codigo v1.1. Aplicar cada mudanca abaixo sem alterar o restante do arquivo:

**Secao "Technology Stack" / "Key Dependencies":**

- Confirmar que lista "@clerk/nextjs" e nao menciona "next-auth" ou "NextAuth"
- Confirmar que lista "GPT-4o" como IA de visao (nao "Claude Vision")
- Adicionar "@mediapipe/tasks-vision" se nao estiver listado (Hand Landmarker client-side)
- Remover qualquer referencia a "R2", "Cloudflare R2", "photo storage"
- Confirmar "Prisma 7" (versao exata do package.json)

**Secao "Configuration":**

- Verificar que nao menciona variaveis de NextAuth (NEXTAUTH_SECRET, NEXTAUTH_URL, etc.)
- Manter CLERK_SECRET_KEY e NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY como estao
- Confirmar que DATABASE_URL e Neon (PostgreSQL serverless)

**Secao "Estrutura do front" (bloco de codigo):**
Verificar que a estrutura listada bate com `src/` real. Atualizar se diferente:

```
src/
  app/              # rotas, layouts, composicao de pagina
  components/
    ui/             # primitives (Button, Input, Card, Badge, Separator...)
    shared/         # blocos compartilhados entre areas
    landing/        # home/institucional
    lp-venda/       # landing page de venda
    reading/        # componentes de leitura (resultado, glyphs, cards)
    camera/         # componentes de camera
    account/        # componentes da area logada
    tarot/          # componentes do tarot
  hooks/            # hooks genericos/transversais
  lib/              # adapters, ponte mock>backend
  mocks/            # mocks isolados
  types/            # contratos de dados
  data/             # blocos estaticos, pacotes de creditos
  proxy.ts          # auth middleware (Clerk clerkMiddleware)
  generated/        # Prisma client gerado (nao editar)
```

**Secao "Project" (bloco de descricao):**

- Substituir qualquer mencao a "NextAuth" por "Clerk"
- Substituir qualquer mencao a "Claude Vision" ou "OpenAI Claude" por "GPT-4o"
- Remover qualquer mencao a "R2" ou armazenamento de fotos
- Atualizar "Esta milestone implementa" para refletir o que v1.1 faz: "A milestone v1.1 alinha o codigo com as decisoes arquiteturais — fluxo unico com is_self flag, MediaPipe Hand Landmarker real, Clerk como unico auth, e limpeza de artefatos obsoletos."

**Secao "Constraints":**

- Confirmar que "Auth: Clerk e source of truth" esta correto (nao NextAuth)
- Adicionar se nao existir: "proxy.ts (nao middleware.ts) e o arquivo de autenticacao Clerk"

Nao alterar: Rules, Commands, Brand voice, Naming, Colors, Fonts, Git, Behavior, Modularizacao, Docs, Colors, Fonts.
</action>

  <verify>
    <automated>grep -c "NextAuth\|next-auth\|Claude Vision\|R2/Cloudflare\|photo storage" CLAUDE.md && echo "COUNT ABOVE MUST BE 0"</automated>
  </verify>

  <done>
    - CLAUDE.md nao menciona NextAuth, Claude Vision, ou R2
    - Stack lista Clerk, GPT-4o, @mediapipe/tasks-vision, Prisma 7
    - Estrutura de pastas inclui proxy.ts e generated/
    - Descricao do projeto reflete decisoes v1.1
  </done>
</task>

</tasks>

<verification>
Apos ambas as tasks:

```bash
# Nenhuma referencia stale em architecture.md
grep -c "share_token\|expires_at\|NextAuth\|next-auth\|R2/Cloudflare\|Claude Vision" docs/architecture.md

# Nenhuma referencia stale em CLAUDE.md
grep -c "NextAuth\|next-auth\|Claude Vision\|R2/Cloudflare" CLAUDE.md

# Build nao quebra (docs nao sao importados, mas garante que nenhum arquivo foi tocado errado)
npm run build 2>&1 | tail -5
```

</verification>

<success_criteria>

- docs/architecture.md: zero ocorrencias de share_token, expires_at (credit_packs), NextAuth, R2, Claude Vision
- docs/architecture.md: schema do banco reflete prisma/schema.prisma real (sem share_token/expires_at)
- docs/architecture.md: fluxo MediaPipe descreve Hand Landmarker com auto-captura 1.5s
- docs/architecture.md: STATUS atualizado para v1.1 implementado
- CLAUDE.md: zero ocorrencias de NextAuth, Claude Vision, R2
- CLAUDE.md: estrutura de pastas inclui proxy.ts e generated/
- npm run build passa sem erros
  </success_criteria>

<output>
Apos conclusao, criar `.planning/phases/05-docs-sync/05-01-SUMMARY.md` com:
- O que foi atualizado em cada arquivo
- Secoes removidas/adicionadas
- Verificacao final (grep counts)
</output>
