---
phase: 01-auditoria
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/report.ts
  - src/lib/reading-client.ts
  - src/app/ler/resultado/[id]/page.tsx
  - src/app/ler/resultado/[id]/completo/page.tsx
  - src/app/ler/resultado/[id]/share/page.tsx
  - src/app/compartilhar/[token]/page.tsx
  - src/app/conta/leituras/page.tsx
  - src/app/conta/leituras/[id]/page.tsx
  - src/components/reading/ElementHero.tsx
  - src/hooks/useStoredName.ts
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-08
  - AUDIT-09

must_haves:
  truths:
    - "Reading type no longer contains share_token or share_expires_at fields"
    - "No file constructs a Reading object with share_token or share_expires_at"
    - "Resultado page works without VALID_MOCK_IDS guard"
    - "ElementHero gets name from API/sessionStorage, not from hardcoded Marina"
  artifacts:
    - path: "src/types/report.ts"
      provides: "Reading interface without share_token/share_expires_at"
      contains: "interface Reading"
    - path: "src/lib/reading-client.ts"
      provides: "getReading returns Reading without obsolete fields"
  key_links:
    - from: "src/lib/reading-client.ts"
      to: "src/types/report.ts"
      via: "imports Reading type"
      pattern: "import.*Reading.*report"
---

<objective>
Remove share_token, share_expires_at, VALID_MOCK_IDS, and fallbackName="Marina" from the codebase.

Purpose: These are artifacts from pre-backend mocks. share_token was never implemented (sharing uses reading UUID directly). expires_at on credits was removed by design decision. VALID_MOCK_IDS was a dev guard that blocks real reading IDs. fallbackName="Marina" should be replaced by the actual name from the API response.
Output: Clean types and pages that work with real backend data only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/types/report.ts
@src/lib/reading-client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove share_token and share_expires_at from Reading type and all consumers</name>
  <files>src/types/report.ts, src/lib/reading-client.ts, src/app/ler/resultado/[id]/completo/page.tsx, src/app/compartilhar/[token]/page.tsx, src/app/conta/leituras/page.tsx, src/app/conta/leituras/[id]/page.tsx, src/app/ler/resultado/[id]/share/page.tsx</files>
  <read_first>src/types/report.ts, src/lib/reading-client.ts, src/app/ler/resultado/[id]/completo/page.tsx, src/app/compartilhar/[token]/page.tsx, src/app/conta/leituras/page.tsx, src/app/conta/leituras/[id]/page.tsx, src/app/ler/resultado/[id]/share/page.tsx</read_first>
  <action>
    1. In `src/types/report.ts`: Remove `share_token: string;` and `share_expires_at: string;` from the `Reading` interface. The interface should keep: id, tier, report, created_at.

    2. In `src/lib/reading-client.ts` function `getReading()`: Remove lines that construct `share_token: r.id` and `share_expires_at: "2099-12-31T00:00:00.000Z"` from the return object (lines 87-88). The return should be `{ id: r.id, tier: r.tier as Reading["tier"], report: r.report, created_at: r.created_at }`.

    3. In `src/app/ler/resultado/[id]/completo/page.tsx` function `CompletoInner`: Remove lines 54-57 where `setData` constructs object with `share_token` and `share_expires_at`. Replace with just `setData(r)` since getReading already returns a proper Reading.

    4. In `src/app/compartilhar/[token]/page.tsx` function `resolveReading`: Remove `share_token: r.id` and `share_expires_at: "2099-12-31T00:00:00.000Z"` from the ShareReading construction (lines 46-47). Update `ShareReading` type alias — it extends Reading which no longer has those fields, so just remove the manual assignment.

    5. In `src/app/conta/leituras/page.tsx` function `LeiturasContent`: Remove `share_token: r.id` and `share_expires_at: "2099-12-31T00:00:00.000Z"` from the mapped Reading construction (lines 387-388).

    6. In `src/app/conta/leituras/[id]/page.tsx`: The `ShareOptions` component receives `token` prop and uses it to build share URL. Change it to receive `readingId` instead. At line 146, change `reading.share_token` to `reading.id`. Rename prop from `token` to `readingId` in ShareOptions.

    7. In `src/app/ler/resultado/[id]/share/page.tsx`: The shareUrl uses `data.share_token`. Change to `data.id` (line 227-228). Since Reading no longer has share_token, this is the natural replacement — sharing uses the reading UUID directly per architecture decision.

  </action>
  <verify>
    <automated>cd "/Users/luana/Documents/Code Projects/MaosFalam" && grep -rn "share_token\|share_expires_at\|expires_at.*2099" src/ --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v ".next" ; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "share_token" src/ --include="*.ts" --include="*.tsx"` returns zero matches
    - `grep -rn "share_expires_at" src/ --include="*.ts" --include="*.tsx"` returns zero matches
    - `Reading` interface in report.ts has exactly 4 fields: id, tier, report, created_at
    - `npm run type-check` passes with zero errors
  </acceptance_criteria>
  <done>No file in src/ references share_token or share_expires_at. Reading type is clean. All pages compile.</done>
</task>

<task type="auto">
  <name>Task 2: Remove VALID_MOCK_IDS and fallbackName="Marina"</name>
  <files>src/app/ler/resultado/[id]/page.tsx, src/app/ler/resultado/[id]/completo/page.tsx, src/components/reading/ElementHero.tsx, src/hooks/useStoredName.ts</files>
  <read_first>src/app/ler/resultado/[id]/page.tsx, src/app/ler/resultado/[id]/completo/page.tsx, src/components/reading/ElementHero.tsx, src/hooks/useStoredName.ts</read_first>
  <action>
    1. In `src/app/ler/resultado/[id]/page.tsx`:
       - Delete the `VALID_MOCK_IDS` constant (line 19) and its comment (line 18).
       - Delete the guard block in `ResultadoPage` that checks `VALID_MOCK_IDS.has(id)` (lines 150-156). The `ResultadoInner` component already handles not-found via `getReading()` returning null, so the guard is redundant now that the backend exists.
       - Change `fallbackName="Marina"` to remove the prop entirely. ElementHero should get the name from the reading report, not a hardcoded fallback.

    2. In `src/app/ler/resultado/[id]/completo/page.tsx`:
       - Change `fallbackName="Marina"` to remove the prop entirely (line 88).

    3. In `src/components/reading/ElementHero.tsx`:
       - Remove `fallbackName` from the Props interface. Replace it with an optional `targetName` prop: `targetName?: string`.
       - Change the name resolution: `const name = useStoredName() ?? targetName ?? "Voce";`. This way: sessionStorage name takes priority, then the name from the API, then a generic "Voce" (never "Marina").
       - Pass `targetName` from the result pages by extracting it from the report API response. In resultado/[id]/page.tsx, the report does not have target_name directly, but the API returns it — update getReading to include target_name.

    4. Actually, simpler approach for ElementHero: The reading API already returns target_name. In `src/lib/reading-client.ts`, add `target_name` to the returned Reading object. Then:
       - Add `target_name?: string` to the Reading interface in report.ts
       - In getReading, return `target_name: r.target_name`
       - In resultado pages, pass `targetName={report.target_name ?? undefined}` (but report is ReportJSON which doesn't have target_name — it's on the reading level)
       - Better: pass `targetName={data?.target_name}` from the reading response

    5. Updated plan for ElementHero:
       - Add `target_name?: string` field to Reading interface in report.ts (optional)
       - In reading-client.ts getReading, add `target_name: r.target_name` to return
       - In ElementHero.tsx: rename `fallbackName` prop to `targetName?: string`. Change name resolution to `const name = useStoredName() ?? targetName ?? "Voce";`
       - In resultado/[id]/page.tsx: remove fallbackName prop, pass the reading's name if available. Since ResultadoInner has the report but not reading-level data, refactor to keep the full Reading object and pass `targetName={reading.target_name}` where `reading` is the Reading from getReading.
       - In completo/page.tsx: change `fallbackName="Marina"` to `targetName={data.target_name}`

    6. In `src/hooks/useStoredName.ts`:
       - Remove the `fallbackName` parameter. The hook should just return `string | null`. Change signature to `export default function useStoredName(): string | null`.
       - Remove `fallbackName` from useState init: `const [name, setName] = useState<string | null>(null);`
       - Check if any other file calls useStoredName with an argument and update those calls.

  </action>
  <verify>
    <automated>cd "/Users/luana/Documents/Code Projects/MaosFalam" && grep -rn "VALID_MOCK_IDS\|fallbackName\|\"Marina\"" src/ --include="*.ts" --include="*.tsx" | grep -v "Depoimentos" | grep -v node_modules ; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "VALID_MOCK_IDS" src/` returns zero matches
    - `grep -rn "fallbackName" src/` returns zero matches
    - `grep -rn '"Marina"' src/ --include="*.tsx"` only matches src/components/lp-venda/Depoimentos.tsx (testimonial author name, legitimate)
    - useStoredName hook takes no parameters
    - ElementHero uses targetName prop (optional) with fallback to "Voce"
    - `npm run type-check` passes
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>VALID_MOCK_IDS deleted. fallbackName="Marina" replaced by targetName from API data. useStoredName is parameterless. Build passes.</done>
</task>

</tasks>

<verification>
```bash
# All audit items for this plan
grep -rn "share_token\|share_expires_at" src/ --include="*.ts" --include="*.tsx"   # expect: 0 matches
grep -rn "VALID_MOCK_IDS" src/                                                      # expect: 0 matches
grep -rn "fallbackName" src/                                                        # expect: 0 matches
grep -rn '"Marina"' src/ --include="*.tsx" | grep -v Depoimentos                    # expect: 0 matches
npm run type-check                                                                  # expect: pass
npm run build                                                                       # expect: pass
```
</verification>

<success_criteria>

- Zero references to share_token, share_expires_at in src/
- Zero references to VALID_MOCK_IDS in src/
- Zero references to fallbackName in src/
- "Marina" only in Depoimentos.tsx (testimonial)
- TypeScript and build both pass
  </success_criteria>

<output>
After completion, create `.planning/phases/01-auditoria/01-01-SUMMARY.md`
</output>
