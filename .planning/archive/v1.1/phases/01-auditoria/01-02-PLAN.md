---
phase: 01-auditoria
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/ler/resultado/[id]/completo/page.tsx
  - src/app/ler/nome/page.tsx
  - src/hooks/useCameraPipeline.ts
  - src/app/conta/perfil/page.tsx
  - src/app/conta/leituras/page.tsx
  - src/components/reading/UpsellSection.tsx
  - docs/exemplo-estrutura.md
  - docs/blocos-resultado.md
autonomous: true
requirements:
  - AUDIT-03
  - AUDIT-04
  - AUDIT-05
  - AUDIT-06
  - AUDIT-07
  - AUDIT-10
  - AUDIT-11

must_haves:
  truths:
    - "No reference to NextAuth, R2, Cloudflare, Claude Vision, or Planeta dominante exists in src/"
    - "Section order in completo page matches v2 spec"
    - "No obsolete TODOs remain — only TODOs for future phases are kept"
    - "No login()/register() dead stubs exist in hooks"
  artifacts:
    - path: "src/app/ler/resultado/[id]/completo/page.tsx"
      provides: "Correct section ordering per v2 spec"
    - path: "docs/exemplo-estrutura.md"
      provides: "Monte dominante instead of Planeta dominante"
  key_links:
    - from: "src/app/ler/resultado/[id]/completo/page.tsx"
      to: "src/types/report.ts"
      via: "Renders sections in v2 order"
      pattern: "Cruzamentos.*Compatibilidade.*Raros"
---

<objective>
Verify already-clean items (NextAuth, R2, Claude Vision, login stubs), fix section ordering in completo page, replace "Planeta dominante" in docs, and clean obsolete TODOs.

Purpose: AUDIT-03, AUDIT-04, AUDIT-05, AUDIT-10 are already clean in src/ (verified by grep). AUDIT-06 needs doc fixes. AUDIT-07 needs section reordering. AUDIT-11 needs TODO cleanup.
Output: Fully audited codebase with correct section ordering and no stale references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/ler/resultado/[id]/completo/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix section ordering in completo page and rename Planeta dominante in docs</name>
  <files>src/app/ler/resultado/[id]/completo/page.tsx, docs/exemplo-estrutura.md, docs/blocos-resultado.md</files>
  <read_first>src/app/ler/resultado/[id]/completo/page.tsx, docs/exemplo-estrutura.md, docs/blocos-resultado.md</read_first>
  <action>
    **AUDIT-07 — Section ordering in completo/page.tsx:**

    The v2 spec order is: Prologo > Coracao > Paywall > Cabeca > Vida > Venus > Montes > Destino > Cruzamentos > Compatibilidade > Raros > Epilogo

    Current completo page renders: Sections (iterated) > Venus > Compatibility (07) > Crossings (08) > Rare Signs (09) > Epilogue

    Fix: Swap the order of Compatibility and Crossings sections. Move the Crossings block (currently lines 141-225) BEFORE the Compatibility block (currently lines 133-138).

    Update the hardcoded section numbers:
    - Crossings: change `number="08"` to `number="07"`, label stays "Onde as linhas se encontram"
    - Compatibility: change `number="07"` to `number="08"`, label stays "Com quem você casa"
    - Rare Signs: stays `number="09"`

    **AUDIT-06 — Planeta dominante in docs:**

    In `docs/exemplo-estrutura.md`:
    - Line 50: already says "Monte dominante" — this is correct, keep it
    - Line 553: says '"Planeta dominante" no Retrato da Mão' — this is a "what NOT to do" reference in a checklist. Keep as-is since it's documenting the wrong pattern to avoid.
    - Line 567: says '"Monte dominante" em vez de "Planeta dominante"' — this is documenting the correct pattern. Keep as-is.

    In `docs/blocos-resultado.md`:
    - Line 1330: table row `"Planeta dominante" | "Monte dominante"` — this is a migration table showing old vs new. Keep as-is since it documents the change.

    Verify no src/ files use "Planeta dominante" (already confirmed by grep — zero matches in src/).

    **AUDIT-05 — Claude Vision:**

    Verify no src/ files reference "Claude Vision" (already confirmed — zero matches in src/). The references are only in planning docs and ROADMAP (describing what to audit), which is correct.

    **AUDIT-03 — NextAuth:**
    Verify no src/ files reference NextAuth, useSession, getServerSession (already confirmed — zero matches).

    **AUDIT-04 — R2/Cloudflare:**
    Verify no src/ files reference R2, Cloudflare, photo_key, photoKey (already confirmed — zero matches).

    **AUDIT-10 — login()/register() stubs:**
    Verify no hooks have login() or register() function stubs (already confirmed — zero matches in src/hooks/).

  </action>
  <verify>
    <automated>cd "/Users/luana/Documents/Code Projects/MaosFalam" && grep -n "number=\"07\"\|number=\"08\"\|number=\"09\"" src/app/ler/resultado/\\[id\\]/completo/page.tsx && echo "---" && grep -rn "Planeta dominante" src/ --include="*.ts" --include="*.tsx" && echo "---" && grep -rn "Claude Vision\|NextAuth\|next-auth\|useSession\|getServerSession" src/ --include="*.ts" --include="*.tsx" && echo "---" && grep -rn "R2\|Cloudflare\|photo_key\|photoKey" src/ --include="*.ts" --include="*.tsx" ; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - In completo/page.tsx, Crossings section appears BEFORE Compatibility section in the JSX
    - Crossings has number="07", Compatibility has number="08", Rare Signs has number="09"
    - `grep -rn "Planeta dominante" src/` returns zero matches
    - `grep -rn "Claude Vision" src/` returns zero matches
    - `grep -rn "NextAuth\|next-auth\|useSession\|getServerSession" src/` returns zero matches
    - `grep -rn "R2\|Cloudflare\|photo_key\|photoKey" src/` returns zero matches
    - `grep -rn "login()\|register()" src/hooks/` returns zero matches
  </acceptance_criteria>
  <done>Section order matches v2 spec. No stale references to removed systems in src/. Docs reference "Planeta dominante" only in migration/checklist context (documenting the old pattern).</done>
</task>

<task type="auto">
  <name>Task 2: Clean obsolete TODOs</name>
  <files>src/app/ler/nome/page.tsx, src/hooks/useCameraPipeline.ts, src/app/conta/perfil/page.tsx, src/app/conta/leituras/page.tsx, src/components/reading/UpsellSection.tsx</files>
  <read_first>src/app/ler/nome/page.tsx, src/hooks/useCameraPipeline.ts, src/app/conta/perfil/page.tsx, src/app/conta/leituras/page.tsx, src/components/reading/UpsellSection.tsx</read_first>
  <action>
    Evaluate each TODO found in src/ and remove obsolete ones. Keep TODOs that reference future phase work still not implemented.

    1. `src/app/conta/leituras/page.tsx` line 74:
       `// TODO: when backend exists, compare with reading.target_name from DB.`
       The backend NOW exists and the API returns target_name. Remove this TODO and the `void _currentUserName;` line. Update `getVariant` to actually compare: if `reading.target_name` differs from `currentUserName`, return "for_other". Change the function to:
       ```
       function getVariant(reading: Reading, currentUserName: string): "active_free" | "active_premium" | "for_other" {
         if (reading.target_name && reading.target_name !== currentUserName) return "for_other";
         if (reading.tier === "premium") return "active_premium";
         return "active_free";
       }
       ```
       Note: Reading type in this file extends BaseReading with `target_name?: string`. This field is already being populated from the API response in the same file's useEffect.

    2. `src/hooks/useCameraPipeline.ts` line 14-15:
       `TODO: substituir por MediaPipe real + detecção de landmarks quando integrar com o backend.`
       This is NOT obsolete — it's Phase 3 work (MP-02). Update the TODO to reference the phase:
       `// Phase 3 (MP-02): substituir por MediaPipe Hand Landmarker real com detecção de landmarks.`

    3. `src/app/conta/perfil/page.tsx` lines 22-26:
       `TODO (backend): Editar nome: PATCH /api/user { name }, Trocar senha: POST /api/auth/change-password`
       This is NOT obsolete — it's Phase 4 work (CLK-03, CLK-04). But the approach changed: Clerk UserProfile handles these. Update:
       `// Phase 4 (CLK-03, CLK-04): delegar edicao de nome e troca de senha ao Clerk UserProfile.`

    4. `src/components/reading/UpsellSection.tsx` lines 13-14:
       `TODO (backend): o id da leitura atual devia vir do contexto da URL/rota em vez de sessionStorage.`
       The backend exists. The reading id IS already in the URL (the page is at /ler/resultado/[id]). This TODO is partially obsolete. The UpsellSection reads from pathname which works. Remove the TODO comment since the current approach (reading from pathname) is adequate.

    5. `src/app/ler/nome/page.tsx` lines 12-28:
       Large TODO block about email validation flow. The backend exists but this specific email-check flow is NOT yet implemented (no GET /api/auth/check-email endpoint exists). This is future work. Trim to a concise reference:
       `// Future: verificar se email ja tem conta via API antes de prosseguir (redirect pra login se sim).`

  </action>
  <verify>
    <automated>cd "/Users/luana/Documents/Code Projects/MaosFalam" && grep -n "TODO" src/ -r --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v ".next" ; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "TODO (backend)" src/` returns zero matches (all "(backend)" TODOs either removed or rewritten as phase references)
    - Remaining TODOs reference specific future phases (Phase 3, Phase 4) or are concise future notes
    - The `getVariant` function in conta/leituras/page.tsx compares target_name with currentUserName and returns "for_other" when different
    - `void _currentUserName` removed from getVariant
    - `npm run type-check` passes
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>All obsolete TODOs removed or updated to reference future phases. getVariant uses real backend data for "for_other" detection. Build passes.</done>
</task>

</tasks>

<verification>
```bash
# Section ordering
grep -n "Cruzamento\|Compatibilidade\|Raros" src/app/ler/resultado/\[id\]/completo/page.tsx

# No stale references

grep -rn "NextAuth\|next-auth\|useSession\|getServerSession" src/ --include="_.ts" --include="_.tsx"
grep -rn "R2\|Cloudflare\|photo_key\|photoKey" src/ --include="_.ts" --include="_.tsx"
grep -rn "Claude Vision" src/ --include="_.ts" --include="_.tsx"
grep -rn "Planeta dominante" src/ --include="_.ts" --include="_.tsx"
grep -rn "login()\|register()" src/hooks/

# TODOs

grep -rn "TODO (backend)" src/ --include="_.ts" --include="_.tsx"

# Build

npm run type-check
npm run build

```
</verification>

<success_criteria>
- Crossings before Compatibility in completo page, numbered 07/08
- Zero references to NextAuth, R2, Claude Vision, Planeta dominante in src/
- Zero login()/register() stubs in hooks
- All obsolete TODOs removed or converted to phase references
- TypeScript and build pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-auditoria/01-02-SUMMARY.md`
</output>
```
