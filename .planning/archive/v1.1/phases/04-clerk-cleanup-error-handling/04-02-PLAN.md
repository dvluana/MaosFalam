---
phase: 04-clerk-cleanup-error-handling
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/conta/leituras/page.tsx
  - src/app/ler/resultado/[id]/page.tsx
autonomous: true
requirements: [DOCS-03, DOCS-04]

must_haves:
  truths:
    - "Quando a API falha em /conta/leituras, o usuario ve um toast na voz da cigana em vez de silencio"
    - "Leitura nao encontrada (getReading retorna null) mostra mensagem distinta de erro de servidor (getReading lanca excecao)"
    - "A mensagem de 404 convida o usuario a comecar uma nova leitura"
    - "A mensagem de 500 pede pra tentar de novo, sem mencionar termos tecnicos"
  artifacts:
    - path: src/app/conta/leituras/page.tsx
      provides: "Toast de erro quando Promise.all falha"
      contains: "showToast"
    - path: src/app/ler/resultado/[id]/page.tsx
      provides: "Estado serverError separado de notFound"
      contains: "serverError"
  key_links:
    - from: src/app/conta/leituras/page.tsx
      to: src/components/ui/ToastProvider.tsx
      via: "useToast() hook"
      pattern: "useToast"
    - from: src/app/ler/resultado/[id]/page.tsx
      to: src/lib/reading-client.ts
      via: "getReading retorna null (404) ou lanca erro (5xx)"
      pattern: "serverError"
---

<objective>
Adicionar feedback de erro visivel em duas paginas: /conta/leituras mostra toast quando a API falha ao carregar leituras; /ler/resultado/[id] diferencia leitura nao encontrada (404) de erro de servidor (5xx).

Purpose: Erros silenciosos deixam o usuario confuso. A cigana fala mesmo quando algo da errado.
Output: Dois arquivos modificados. Erros visiveis e na voz da cigana.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<interfaces>
<!-- useToast hook (src/components/ui/ToastProvider.tsx): -->
```typescript
import { useToast } from "@/components/ui/ToastProvider";

const { showToast } = useToast();

showToast({
variant: "rose", // "gold" | "rose" | "violet"
icon: "♀", // opcional, unicode planetario
message: "Mensagem na voz da cigana.", // Cormorant italic
detail: "DETALHE TECNICO", // opcional, JetBrains Mono
});

````

<!-- getReading (src/lib/reading-client.ts): -->
```typescript
// Retorna null em 404 / 410
// Lanca Error em qualquer outro status (5xx, rede, etc.)
export async function getReading(id: string): Promise<Reading | null>
````

<!-- Estado atual de /conta/leituras Promise.all: -->

```typescript
Promise.all([getUserProfile(), getUserReadings(), getCredits()])
  .then(([profile, data, creditsData]) => {
    // ... set state
    setLoading(false);
  })
  .catch(() => {
    setLoading(false); // silencioso — precisa de toast aqui
  });
```

<!-- Estado atual de /ler/resultado/[id]: -->

```typescript
const [notFound, setNotFound] = useState(false);
// ...
getReading(id)
  .then((r) => {
    if (!r) { setNotFound(true); ... return; }
    // ... set reading
  })
  .catch(() => {
    setNotFound(true);  // errado — precisa de serverError separado
  });
```

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Toast de erro em /conta/leituras quando API falha</name>
  <files>src/app/conta/leituras/page.tsx</files>
  <action>
    Adicionar feedback de erro visivel quando o Promise.all de carregamento de dados falha (per DOCS-03).

    Mudancas em LeiturasContent:
    1. Importar `useToast` de `@/components/ui/ToastProvider`
    2. Chamar `const { showToast } = useToast()` dentro de LeiturasContent
    3. No bloco `.catch()` do Promise.all, chamar showToast antes de setLoading(false):
       ```typescript
       .catch(() => {
         showToast({
           variant: "rose",
           message: "Nao consegui trazer suas leituras. Tenta de novo.",
         });
         setLoading(false);
       });
       ```
    4. Adicionar novo estado `error` ao tipo State e ao array STATES:
       ```typescript
       const STATES = ["has_readings", "empty", "loading", "error"] as const;
       ```
    5. Quando o catch ocorre, definir state "error" alem de exibir o toast:
       ```typescript
       .catch(() => {
         showToast({
           variant: "rose",
           message: "Nao consegui trazer suas leituras. Tenta de novo.",
         });
         setLoading(false);
         // currentState computado: se loading=false e readings.length===0, cai em "empty"
         // nao e necessario um estado error separado pois o toast ja comunica o erro
       });
       ```
    Nota: o estado "empty" e o correto para exibir apos erro (lista vazia), e o toast complementa com a mensagem de erro. Nao e necessario criar um estado de erro separado na UI — o toast e suficiente.

    Nao alterar nenhuma outra logica da pagina. Nao remover StateSwitcher.

  </action>
  <verify>
    <automated>npm run type-check 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <done>
    LeiturasContent importa e usa useToast.
    .catch() exibe toast com mensagem na voz da cigana.
    npm run type-check passa.
  </done>
</task>

<task type="auto">
  <name>Task 2: Diferenciar 404 de 500 em /ler/resultado/[id]</name>
  <files>src/app/ler/resultado/[id]/page.tsx</files>
  <action>
    Separar o estado de erro em dois: leitura nao encontrada (getReading retorna null = 404) e erro de servidor (getReading lanca excecao = 5xx ou falha de rede) (per DOCS-04).

    Mudancas em ResultadoInner:
    1. Adicionar estado serverError:
       ```typescript
       const [serverError, setServerError] = useState(false);
       ```
    2. No .catch(), usar setServerError em vez de setNotFound:
       ```typescript
       .catch(() => {
         setServerError(true);
         setLoading(false);
       });
       ```
    3. Adicionar componente ServerError (proximo de InvalidReading, antes de ResultadoInner):
       ```typescript
       function ServerError() {
         return (
           <main className="min-h-dvh bg-black flex items-center justify-center px-6">
             <div className="text-center max-w-sm">
               <p className="font-cormorant italic text-[22px] text-bone leading-snug mb-6">
                 Eu preciso de um momento. Volte em breve.
               </p>
               <a
                 href={typeof window !== "undefined" ? window.location.href : "#"}
                 className="font-body text-[10px] uppercase tracking-[0.06em] text-bone border border-gold/10 rounded-[0_6px_0_6px] px-10 py-4 inline-block hover:bg-violet/5 transition-colors"
               >
                 Tentar de novo
               </a>
             </div>
           </main>
         );
       }
       ```
    4. Atualizar a logica de renderizacao em ResultadoInner:
       ```typescript
       if (loading) return <PageLoading />;
       if (serverError) return <ServerError />;
       if (notFound || !reading) return <InvalidReading />;
       ```

    Corrigir tambem os textos de InvalidReading para usar acentuacao correta (atualmente tem "nao existe", "comecar" sem acento — corrigir pra "nao existe", "comecar" com acento correto conforme brand voice). Texto atual: "Essa leitura nao existe. Mas a sua pode comecar agora." — manter essa frase, apenas corrigir os caracteres: "Essa leitura nao existe. Mas a sua pode comecar agora." — verificar se ha problemas de encoding e corrigir se necessario.

    Nao alterar nenhuma outra logica. Nao remover ResultStateSwitcher.

  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    ResultadoInner tem estado serverError separado de notFound.
    Catch do getReading define serverError=true.
    Componente ServerError renderiza mensagem distinta na voz da cigana.
    npm run build passa.
  </done>
</task>

</tasks>

<verification>
npm run build
Ambas as paginas compilam sem erros.
grep -n "serverError" src/app/ler/resultado/\[id\]/page.tsx — confirma estado separado.
grep -n "showToast" src/app/conta/leituras/page.tsx — confirma toast importado e chamado.
</verification>

<success_criteria>

- /conta/leituras: .catch() chama showToast com mensagem na voz da cigana (per DOCS-03)
- /ler/resultado/[id]: getReading null -> InvalidReading, getReading throw -> ServerError (per DOCS-04)
- npm run build passa sem erros
  </success_criteria>

<output>
Apos conclusao, criar `.planning/phases/04-clerk-cleanup-error-handling/04-02-SUMMARY.md`
</output>
