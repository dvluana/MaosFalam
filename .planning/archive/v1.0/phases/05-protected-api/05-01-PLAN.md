---
phase: 05-protected-api
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/reading/new/route.ts
  - src/app/api/user/credits/route.ts
  - src/app/api/user/readings/route.ts
  - src/app/api/user/profile/route.ts
  - src/app/api/user/account/route.ts
autonomous: true
requirements: [API-05, API-06, API-07, API-08, API-09, API-10, SEC-03]

must_haves:
  truths:
    - "POST /api/reading/new debits 1 credit FIFO atomically and returns 402 when no credits exist"
    - "GET /api/user/credits returns balance and pack list with no PII in logs"
    - "GET /api/user/readings returns only active readings for the authenticated user"
    - "GET /api/user/profile returns Clerk name/email plus DB cpf/phone"
    - "PUT /api/user/profile upserts cpf and phone without logging their values"
    - "DELETE /api/user/account with confirm='EXCLUIR' soft-deletes; without it returns 400"
    - "Rate limit infrastructure (rateLimit fn) already supports keying by userId for future credits/purchase"
  artifacts:
    - path: src/app/api/reading/new/route.ts
      provides: Credit debit route
      contains: "$transaction"
    - path: src/app/api/user/credits/route.ts
      provides: Balance + pack list
      contains: "balance"
    - path: src/app/api/user/readings/route.ts
      provides: Reading history
      contains: "isActive: true"
    - path: src/app/api/user/profile/route.ts
      provides: Profile CRUD
      contains: "upsert"
    - path: src/app/api/user/account/route.ts
      provides: Soft delete
      contains: "EXCLUIR"
  key_links:
    - from: src/app/api/reading/new/route.ts
      to: prisma.creditPack
      via: $transaction with FIFO findFirst orderBy createdAt asc
      pattern: "\\$transaction"
    - from: src/app/api/user/profile/route.ts
      to: prisma.userProfile
      via: upsert
      pattern: "userProfile.upsert"
---

<objective>
Audit all 5 protected API route files and apply any required fixes before writing tests.

Purpose: Routes were pre-written; this plan verifies correctness against all requirements (Zod v4 syntax, correct auth helper usage, no PII in logs, SEC-03 rate-limit comment) and fixes any issues found.
Output: 5 production-ready route files that pass type-check and lint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/server/lib/auth.ts
@src/server/lib/rate-limit.ts
@prisma/schema.prisma
</context>

<interfaces>
<!-- Auth helpers — use getClerkUserId() for all protected routes unless name/email needed -->
From src/server/lib/auth.ts:
```typescript
// Use for every protected route (session check only, no network call)
export async function getClerkUserId(): Promise<string>
// Use ONLY when name/email needed (makes Clerk API call)
export async function getClerkUser(): Promise<{ id: string; name: string; email: string }>
```

<!-- Rate limit — SEC-03: credits/purchase will use key `user:${userId}` when implemented -->

From src/server/lib/rate-limit.ts:

```typescript
export function rateLimit(key: string, limit: number, windowMs?: number): boolean;
```

<!-- Prisma models relevant to protected routes -->

From prisma/schema.prisma (camelCase after Prisma generation):

```
CreditPack: { id, clerkUserId, packType, total, remaining, createdAt }
Reading:    { id, clerkUserId, targetName, tier, report, isActive, createdAt }
UserProfile:{ clerkUserId, leadId, cpf, phone, createdAt }
```

<!-- Zod v4 key differences from v3 (from STATE.md decisions) -->

// z.string().email() is NOT valid in Zod v4 — use z.email()
// z.string().uuid() is NOT valid in Zod v4 — use z.uuid()
// z.literal("EXCLUIR") — same in v3 and v4, OK
// z.string().min().max() — same in v3 and v4, OK
// z.enum([...]) — same in v3 and v4, OK
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix reading/new and user/credits routes</name>
  <files>
    src/app/api/reading/new/route.ts
    src/app/api/user/credits/route.ts
  </files>
  <action>
    Read both files first (they are already partially shown in context above — re-read to confirm current content before any edits).

    For src/app/api/reading/new/route.ts, verify and fix:
    1. Schema uses z.string().min/max and z.enum — these are Zod v4 compatible, no change needed.
    2. Uses getClerkUserId() — correct (no name/email needed).
    3. $transaction with FIFO findFirst({ orderBy: { createdAt: 'asc' }, where: { clerkUserId, remaining: { gt: 0 } } }) — correct.
    4. Returns 402 when no pack found — correct.
    5. logger.info logs { clerkUserId, target: data.target_name, credits_remaining } — "target_name" is the reading subject's name (not user PII like email/CPF), which is acceptable per SEC-07. No change needed.
    6. Add a comment above the catch block: `// SEC-03: POST /api/credits/purchase rate limit (5/hour by userId) deferred to v2 — rateLimit(key, limit) supports it`

    Wait — SEC-03 comment belongs in a credits/purchase route stub if needed, NOT in reading/new. Instead, add a comment at the top of rate-limit.ts usage pattern OR create a stub file only if the route does not exist. Check if src/app/api/credits/purchase/route.ts exists. If it does not exist, create a minimal stub that returns 501 with a comment about SEC-03 deferral. If it already exists, leave it alone.

    For src/app/api/user/credits/route.ts, verify and fix:
    1. GET handler uses getClerkUserId() — correct.
    2. packs.map uses explicit type annotation — this works but consider removing the explicit type annotation since Prisma generates typed results. Either approach passes type-check; keep as-is if it compiles.
    3. No PII in logger.error call — correct (only logs `route`).
    4. Math.max(0, p.remaining) in balance calculation — defensive, correct.

    Run: npm run type-check 2>&1 | grep -E "reading/new|user/credits" to verify no errors in these files.

  </action>
  <verify>
    <automated>cd "/Users/luana/Documents/Code Projects/MaosFalam" && npm run type-check 2>&1 | grep -E "src/app/api/reading/new|src/app/api/user/credits" || echo "No type errors in these files"</automated>
  </verify>
  <done>
    Both route files pass type-check. reading/new has $transaction FIFO. user/credits returns balance + packs. No PII in logs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix user/readings, user/profile, and user/account routes</name>
  <files>
    src/app/api/user/readings/route.ts
    src/app/api/user/profile/route.ts
    src/app/api/user/account/route.ts
  </files>
  <action>
    Read all three files first before any edits.

    For src/app/api/user/readings/route.ts:
    1. Uses getClerkUserId() — correct.
    2. Filters: { clerkUserId, isActive: true } — correct, only active readings for this user.
    3. report field is of type unknown — this passes but consider `r.report as import('@/generated/prisma/client').JsonValue` if type-check fails. Keep as-is unless type errors appear.
    4. No PII logged — correct.

    For src/app/api/user/profile/route.ts:
    1. GET uses getClerkUser() — CORRECT (needs name and email, so network call is justified per JSDoc in auth.ts).
    2. PUT uses getClerkUser() — CORRECT for the same reason (user.id is used for upsert).
    3. updateSchema: z.object({ cpf: z.string().min(11).max(14).optional(), phone: z.string().min(10).max(20).optional() }) — Zod v4 compatible, no change.
    4. logger.info({ clerkUserId: user.id }, "Profile updated") — logs only the ID, no CPF or phone. Correct per SEC-07.
    5. Verify the upsert create block includes all required fields. UserProfile schema requires only clerkUserId as @id — cpf and phone are optional. The create block { clerkUserId: user.id, cpf: data.cpf, phone: data.phone } is valid.

    For src/app/api/user/account/route.ts:
    1. z.literal("EXCLUIR") — Zod v4 compatible. Correct.
    2. Uses getClerkUserId() — correct (no name/email needed).
    3. Soft delete: prisma.reading.updateMany({ where: { clerkUserId }, data: { isActive: false } }) — this matches the success criterion "sets is_active = false". Correct.
    4. logger.info({ clerkUserId }, "Account soft-deleted") — no PII. Correct.

    After review, apply any fixes. Then run type-check on all three.

    IMPORTANT: If src/app/api/credits/purchase/route.ts does NOT exist, create it:
    ```typescript
    // SEC-03: Rate limit 5/hour by userId when implemented (deferred to v2)
    // PAY-01: AbacatePay checkout creation deferred to v2 — see docs/architecture.md
    import { NextResponse } from "next/server";

    export async function POST() {
      return NextResponse.json(
        { error: "Pagamentos nao implementados nesta versao." },
        { status: 501 },
      );
    }
    ```

  </action>
  <verify>
    <automated>cd "/Users/luana/Documents/Code Projects/MaosFalam" && npm run type-check 2>&1 | grep -E "user/readings|user/profile|user/account|credits/purchase" || echo "No type errors in these files"</automated>
  </verify>
  <done>
    All three user routes and the credits/purchase stub pass type-check. Profile PUT does not log CPF or phone. Account DELETE uses z.literal("EXCLUIR"). credits/purchase stub exists and returns 501.
  </done>
</task>

</tasks>

<verification>
cd "/Users/luana/Documents/Code Projects/MaosFalam" && npm run type-check 2>&1 | tail -5
</verification>

<success_criteria>

- All 5 protected routes (reading/new, user/credits, user/readings, user/profile, user/account) pass type-check
- No PII (name, email, CPF, phone) appears in any logger call in these files
- z.literal("EXCLUIR") in account route
- $transaction FIFO in reading/new
- credits/purchase stub exists (SEC-03 deferred)
- npm run lint passes on these files
  </success_criteria>

<output>
After completion, create `.planning/phases/05-protected-api/05-01-SUMMARY.md`
</output>
