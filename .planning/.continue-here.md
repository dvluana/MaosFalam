# Continue Here

**Paused:** 2026-04-14 ~20:00 BRT
**Branch:** develop
**Last commit:** `8d4cf43` security: add amount re-check in webhook

## What Was Done This Session

### v2 Monetização (4 phases complete)

- Phase 12: AbacatePay v2 Backend (wrapper, produtos, webhook)
- Phase 13: Frontend Payment Flow (creditos, initiatePurchase, UpsellSection)
- Phase 14: Email & Hardening (Resend templates, Clerk webhook, opt-in)
- Phase 15: Bug Fixes (acentuação, camera handedness, revelação responsiva)

### Post-v2 Debug & Fixes

- Webhook ROOT CAUSE: AbacatePay v2 nests data inside `data.checkout.xxx` not `data.xxx`
- CPF modal removido — AbacatePay coleta CPF no checkout hosted
- BuyCreditsModal reescrito: seleciona pack → botão comprar → redirect direto pro AbacatePay
- Acentuação: 40+ fixes em 10 arquivos
- 15+ bug fixes (proxy 401, manifesto contraste, login, toast, gender, etc)
- Favicon violeta + título "Staging - MF"
- Signup sem verificação de email (handle status "complete")
- Amount re-check no webhook (security audit recommendation)
- 22 testes E2E + auditoria de segurança (PASSED)

### BD Cleanup

- webhook_debug table dropada
- Payments órfãos limpos
- Credit packs órfãos limpos

## What Remains

### Must verify (next session)

1. **Webhook E2E real** — fazer compra no staging com o novo deploy (data.checkout fix) e confirmar créditos creditam automaticamente sem replay manual
2. **Toast ?purchased=1** — confirmar que aparece uma vez no topo com overlay e não repete no reload
3. **BuyCreditsModal** — testar visualmente (badge popular posição, botão comprar, seleção de pack)

### Pendente

- Clerk Dashboard: renomear "PariTech" → "MaosFalam", remover Facebook OAuth
- Clerk webhook user.created + CLERK_WEBHOOK_SECRET
- Resend: verificar domínio + RESEND_API_KEY
- Share card redesign
- Página /creditos (carrossel) mantida pra fluxo de upsell deslogado
- Menu "Sair" numeração pode ainda mostrar 07 em vez de 05

### Decisions Made

- CPF não coletado por nós — AbacatePay coleta no checkout hosted
- Customer AbacatePay criado 1x, recriado se CPF chegar depois
- Webhook: fallback triplo (externalId → checkoutId → amount)
- Amount re-check no webhook pra prevenir tampering
- Toast no topo com backdrop overlay

## How to Resume

```
/gsd:resume-work
```

Ou manualmente:

1. Verificar deploy staging: `vercel list`
2. Testar compra E2E no staging (criar conta, comprar, verificar créditos)
3. Se créditos não aparecem, checar logs: `mcp__vercel__get_runtime_logs`
4. Continuar com pendências do TODO.md
