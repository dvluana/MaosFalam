---
phase: 02-upload-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/camera/UploadInstructionScreen.tsx
autonomous: true
requirements: [UPL-02]
must_haves:
  truths:
    - "Usuario que escolheu upload ve instrucao de qual mao fotografar antes do file picker abrir"
    - "Dicas de qualidade aparecem abaixo do outline SVG (luz, fundo, palma aberta)"
    - "Botao de voltar permite retornar ao MethodChoice"
    - "Botao de continuar abre o file picker"
  artifacts:
    - path: "src/components/camera/UploadInstructionScreen.tsx"
      provides: "Tela de instrucao de upload com SVG, dicas, e CTA"
      exports: ["default UploadInstructionScreen"]
  key_links:
    - from: "UploadInstructionScreen"
      to: "HandOutlineSVG"
      via: "import direto"
      pattern: "HandOutlineSVG"
    - from: "UploadInstructionScreen"
      to: "camera/page.tsx"
      via: "onContinue callback abre file picker"
      pattern: "onContinue"
---

<objective>
Criar UploadInstructionScreen — tela de instrucao que aparece quando o usuario escolhe "Me mandar a foto" no MethodChoice, antes de abrir o file picker.

Purpose: Dar instrucoes claras sobre qual mao fotografar e como tirar uma boa foto antes do picker abrir. Reduz fotos ruins e rejeicoes de validacao.
Output: src/components/camera/UploadInstructionScreen.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-camera-ui/01-01-SUMMARY.md
@.planning/phases/01-camera-ui/01-02-SUMMARY.md

<interfaces>
<!-- Componentes existentes que UploadInstructionScreen usa -->

From src/components/camera/HandOutlineSVG.tsx:

```typescript
interface HandOutlineSVGProps {
  dominantHand: "right" | "left";
  size?: number; // default 200
  className?: string;
}
export default function HandOutlineSVG(props: HandOutlineSVGProps): JSX.Element;
```

From src/components/ui/Button.tsx (DS):

```typescript
// variant: "primary" | "secondary" | "ghost" | "icon"
// size?: "sm" | "default" | "lg"
export default function Button({ variant, size, onClick, children, ...}: ButtonProps)
```

DS tokens disponíveis via Tailwind:

- Cores: text-bone, text-bone-dim, text-gold, text-violet, text-rose, bg-black, bg-deep
- Fonts: font-cinzel, font-cormorant, font-raleway, font-jetbrains
- Radius brandado: style={{ borderRadius: "0 6px 0 6px" }}
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Criar UploadInstructionScreen</name>
  <files>src/components/camera/UploadInstructionScreen.tsx</files>
  <action>
Criar componente novo. Nao ha arquivo existente — criacao pura.

Props:

```typescript
interface UploadInstructionScreenProps {
  dominantHand: "right" | "left";
  onContinue: () => void; // abre file picker
  onBack: () => void; // volta ao MethodChoice
}
```

Layout (full-screen, igual ao HandInstructionOverlay — min-h-dvh, bg-black, flex flex-col items-center justify-center, px-6):

1. **Eyebrow** (topo): font-jetbrains text-[9px] tracking-[2px] uppercase text-violet opacity-60 — texto: "RETRATO DA PALMA"

2. **HandOutlineSVG** com dominantHand passado, size=180. Adicionar className="opacity-70" para parecer instrucao, nao captura. O SVG tem role="img" e aria-label por padrao.

3. **Frase da cigana** (font-cormorant italic text-[20px] text-bone text-center max-w-xs leading-[1.35]):
   - dominantHand === "right": "Fotografe sua mao direita. Palma aberta, virada pra cima."
   - dominantHand === "left": "Fotografe sua mao esquerda. Palma aberta, virada pra cima."

4. **Dicas de qualidade** — 3 itens horizontais compactos (flex gap-6 justify-center), cada um com:
   - Icone SVG inline pequeno (18px, stroke gold/60)
   - Label font-jetbrains text-[9px] tracking-[1.2px] uppercase text-bone-dim

   Dicas:
   - Icone sol (circulo + raios): "BOA LUZ"
   - Icone fundo (quadrado limpo): "FUNDO LIMPO"
   - Icone palma aberta (mao simplificada): "DEDOS ABERTOS"

   Icones SVG simples inline (nao importar lib externa):
   - Sol: `<circle cx="9" cy="9" r="3.5"/>` + 6 linhas de raio de 2px a partir de r=5.5
   - Fundo: `<rect x="3" y="3" width="12" height="12" rx="1"/>` (quadrado)
   - Palma: 4 linhas verticais curtas (dedos) + base horizontal (palma)

5. **Botoes** (flex gap-3 w-full max-w-xs):
   - Button variant="ghost" onClick={onBack}: "Voltar"
   - Button variant="primary" onClick={onContinue}: "Escolher foto"

Animacao de entrada: motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ duration: 0.5 }}.

Sem estado interno — componente puramente de apresentacao com callbacks.

IMPORTANTE: `"use client"` no topo. Import order: react, framer-motion, @/components/\*, tipos locais. Blank line entre grupos. Trailing comma.
</action>
<verify>
<automated>cd "/Users/luana/Documents/Code Projects/MaosFalam" && npx tsc --noEmit 2>&1 | head -20 && npm run lint 2>&1 | grep -E "error|warning" | grep "UploadInstructionScreen" || echo "Lint OK for file"</automated>
</verify>
<done> - src/components/camera/UploadInstructionScreen.tsx existe e exporta default - Props: dominantHand, onContinue, onBack - Renderiza HandOutlineSVG, frase correta por dominantHand, 3 dicas, 2 botoes - npx tsc --noEmit passa sem erros no arquivo - npm run lint passa sem erros no arquivo
</done>
</task>

</tasks>

<verification>
```bash
cd "/Users/luana/Documents/Code Projects/MaosFalam"
npx tsc --noEmit
npm run lint
```
Ambos passam sem erros.
</verification>

<success_criteria>

- UploadInstructionScreen.tsx criado e tipado
- Frase correta pro dominantHand escolhido
- HandOutlineSVG integrado com flip por dominantHand
- 3 dicas de qualidade visiveis
- Dois botoes funcionais via callbacks (sem logica interna)
- Zero erros TS, zero erros lint
  </success_criteria>

<output>
Apos conclusao, criar `.planning/phases/02-upload-pipeline/02-01-SUMMARY.md`
</output>
