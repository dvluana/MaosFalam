---
phase: 02-upload-pipeline
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - src/app/ler/camera/page.tsx
  - src/components/camera/UploadPreview.tsx
autonomous: true
requirements: [UPL-01, UPL-03, UPL-04, UPL-05, UPL-06]
must_haves:
  truths:
    - "Clicar em 'Me mandar a foto' no MethodChoice leva para UploadInstructionScreen, nao diretamente ao file picker"
    - "Clicar em 'Escolher foto' na instrucao abre o file picker; arquivo selecionado vai pra validacao"
    - "Validacao progressiva aparece imediatamente apos selecionar arquivo"
    - "Tela de confirmacao aparece com preview apos validacao completa"
    - "Confirmar na tela de confirmacao salva no sessionStorage e redireciona pra /ler/scan"
    - "Arquivo GIF/SVG/BMP/PDF rejeitado com mensagem da cigana, sem prosseguir"
  artifacts:
    - path: "src/app/ler/camera/page.tsx"
      provides: "Camera page com fluxo upload multi-step completo"
    - path: "src/components/camera/UploadPreview.tsx"
      provides: "Componente stub mantido mas substituido pelo novo fluxo"
  key_links:
    - from: "camera/page.tsx showUpload state"
      to: "UploadInstructionScreen → file picker → UploadValidationScreen → UploadConfirmScreen"
      via: "uploadStep state machine: 'instruction' | 'validating' | 'confirm'"
      pattern: "uploadStep"
    - from: "UploadConfirmScreen onConfirm"
      to: "sessionStorage.setItem + router.push('/ler/scan')"
      via: "handleUploadConfirm callback"
      pattern: "maosfalam_photo"
---

<objective>
Conectar os novos componentes de upload na camera page. Substituir o fluxo antigo (showUpload -> UploadPreview diretamente) por um fluxo multi-step: instrucao → file picker → validacao → confirmacao.

Purpose: Fecha o loop da Phase 2 — todos os 6 requirements de upload funcionando end-to-end na camera page.
Output: camera page rewired, UploadPreview descontinuado (mantido como stub vazio para nao quebrar imports, mas sem uso real)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-camera-ui/01-02-SUMMARY.md
@.planning/phases/02-upload-pipeline/02-01-SUMMARY.md
@.planning/phases/02-upload-pipeline/02-02-SUMMARY.md

<interfaces>
<!-- Estado atual da camera page -->

From src/app/ler/camera/page.tsx (estado atual relevante):

```typescript
// Estados existentes relevantes
const [state, setState] = useState<CamState>(forced ?? "method_choice");
const [showUpload, setShowUpload] = useState(false);
const readingContext = loadReadingContext();
const dominantHand = readingContext?.dominant_hand ?? "right";

// Fluxo atual (a ser substituido):
// showUpload = true → <UploadPreview onConfirm onCancel />
```

Novos componentes a integrar:

```typescript
// De 02-01-PLAN:
import UploadInstructionScreen from "@/components/camera/UploadInstructionScreen";
// props: dominantHand, onContinue, onBack

// De 02-02-PLAN:
import UploadValidationScreen from "@/components/camera/UploadValidationScreen";
// props: checks (ValidationCheck[])

import UploadConfirmScreen from "@/components/camera/UploadConfirmScreen";
// props: result, onConfirm, onRetry, onBack

import { useUploadValidation } from "@/hooks/useUploadValidation";
// retorna: { result, validate, reset }
```

Conversao de File para base64 (mesmo padrao do fluxo antigo):

```typescript
const reader = new FileReader();
reader.onload = () => {
  const dataUrl = reader.result as string;
  const base64 = dataUrl.split(",")[1] ?? "";
  sessionStorage.setItem("maosfalam_photo", base64);
  window.setTimeout(() => router.push("/ler/scan"), 600);
};
reader.readAsDataURL(file);
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar uploadStep state e file picker ref na camera page</name>
  <files>src/app/ler/camera/page.tsx</files>
  <action>
Ler o arquivo atual ANTES de editar: src/app/ler/camera/page.tsx

**Mudancas necessarias:**

1. **Adicionar imports** (manter import order existente — externos antes de internos, alfa dentro de cada grupo):

   ```typescript
   import UploadConfirmScreen from "@/components/camera/UploadConfirmScreen";
   import UploadInstructionScreen from "@/components/camera/UploadInstructionScreen";
   import UploadValidationScreen from "@/components/camera/UploadValidationScreen";
   import { useUploadValidation } from "@/hooks/useUploadValidation";
   ```

   Inserir na posicao alfabetica correta dentro do bloco de imports `@/components/camera/*`.
   Remover import de `UploadPreview` (nao e mais usado).

2. **Adicionar estado uploadStep** apos `const [showUpload, setShowUpload] = useState(false)`:

   ```typescript
   type UploadStep = "instruction" | "validating" | "confirm";
   const [uploadStep, setUploadStep] = useState<UploadStep>("instruction");
   ```

   Nota: `type UploadStep` fica DENTRO da funcao CameraPageInner (tipo local), nao no topo do arquivo.

3. **Adicionar ref do file input** apos os outros refs:

   ```typescript
   const uploadInputRef = useRef<HTMLInputElement | null>(null);
   ```

4. **Adicionar hook useUploadValidation** apos `const dominantHand = ...`:

   ```typescript
   const {
     result: uploadResult,
     validate: validateFile,
     reset: resetUpload,
   } = useUploadValidation(dominantHand);
   ```

5. **Adicionar handleUploadFileSelected** (callback para quando arquivo e selecionado no input):

   ```typescript
   const handleUploadFileSelected = useCallback(
     async (e: React.ChangeEvent<HTMLInputElement>) => {
       const file = e.target.files?.[0];
       if (!file) return;
       setUploadStep("validating");
       await validateFile(file);
       setUploadStep("confirm");
     },
     [validateFile],
   );
   ```

6. **Adicionar handleUploadConfirm** (callback do UploadConfirmScreen):

   ```typescript
   const handleUploadConfirm = useCallback(() => {
     if (!uploadResult.file) return;
     setState("camera_capturing");
     if (typeof navigator !== "undefined" && "vibrate" in navigator) {
       navigator.vibrate?.(120);
     }
     const reader = new FileReader();
     reader.onload = () => {
       const dataUrl = reader.result as string;
       const base64 = dataUrl.split(",")[1] ?? "";
       sessionStorage.setItem("maosfalam_photo", base64);
       window.setTimeout(() => router.push("/ler/scan"), 600);
     };
     reader.readAsDataURL(uploadResult.file);
   }, [uploadResult.file, router]);
   ```

7. **Adicionar handleUploadBack** (volta ao MethodChoice e reseta):

   ```typescript
   const handleUploadBack = useCallback(() => {
     resetUpload();
     setUploadStep("instruction");
     setShowUpload(false);
   }, [resetUpload]);
   ```

8. **Adicionar handleUploadRetry** (volta ao file picker):

   ```typescript
   const handleUploadRetry = useCallback(() => {
     resetUpload();
     setUploadStep("instruction");
     // Reseta o input file para permitir re-selecao do mesmo arquivo
     if (uploadInputRef.current) uploadInputRef.current.value = "";
   }, [resetUpload]);
   ```

9. **Substituir o bloco `{showUpload && <UploadPreview ... />}` no JSX** pelo novo fluxo multi-step:

   Localizar o bloco atual:

   ```tsx
   {showUpload && (
     <UploadPreview
       onConfirm={(file: File) => { ... }}
       onCancel={() => setShowUpload(false)}
     />
   )}
   ```

   Substituir por:

   ```tsx
   {
     showUpload && (
       <>
         {/* File input oculto — acionado programaticamente */}
         <input
           ref={uploadInputRef}
           type="file"
           accept="image/jpeg,image/png,image/webp,image/heic,image/heif"
           className="hidden"
           onChange={handleUploadFileSelected}
         />

         {uploadStep === "instruction" && (
           <UploadInstructionScreen
             dominantHand={dominantHand}
             onContinue={() => uploadInputRef.current?.click()}
             onBack={handleUploadBack}
           />
         )}

         {uploadStep === "validating" && <UploadValidationScreen checks={uploadResult.checks} />}

         {uploadStep === "confirm" && (
           <UploadConfirmScreen
             result={uploadResult}
             onConfirm={handleUploadConfirm}
             onRetry={handleUploadRetry}
             onBack={handleUploadBack}
           />
         )}
       </>
     );
   }
   ```

10. **Atualizar o `onPickUpload` do MethodChoice** para resetar o step ao entrar no fluxo:
    Localizar: `onPickUpload={() => setShowUpload(true)}`
    Substituir por:

    ```tsx
    onPickUpload={() => {
      resetUpload();
      setUploadStep("instruction");
      setShowUpload(true);
    }}
    ```

11. **Remover `handleUploadSelected`** (o callback antigo do fluxo de upload direto na camera page que usava `ChangeEvent<HTMLInputElement>` — linha ~86-104). Nao e mais usado.

12. **Limpar import nao utilizado**: remover `type ChangeEvent` do import de react se nao for mais usado por nenhum outro lugar.

IMPORTANTE: Ler o arquivo completo antes de editar. Manter todos os outros estados, refs, effects e handlers intactos. So modificar o que esta listado acima. Rodar `npm run lint` e `npx tsc --noEmit` ao final.
</action>
<verify>
<automated>cd "/Users/luana/Documents/Code Projects/MaosFalam" && npx tsc --noEmit 2>&1 | head -30 && npm run lint 2>&1 | head -30</automated>
</verify>
<done> - camera/page.tsx importa UploadInstructionScreen, UploadValidationScreen, UploadConfirmScreen, useUploadValidation - uploadStep state ("instruction" | "validating" | "confirm") presente - uploadInputRef presente - showUpload && renderiza o bloco multi-step correto por uploadStep - UploadPreview nao e mais importado - npx tsc --noEmit sem erros - npm run lint sem erros
</done>
</task>

<task type="auto">
  <name>Task 2: Build final e smoke test</name>
  <files>src/components/camera/UploadPreview.tsx</files>
  <action>
Ler src/components/camera/UploadPreview.tsx antes de editar.

UploadPreview nao e mais usado pela camera page. Mas para nao quebrar qualquer import residual, manter o arquivo com um stub minimo que nao causa erros de compilacao. Se o arquivo nao tiver outros importadores (verificar com grep), pode limpar pra stub simples:

```typescript
"use client";

// Deprecated: replaced by UploadInstructionScreen + UploadValidationScreen + UploadConfirmScreen flow
// Mantido para nao quebrar imports residuais

export default function UploadPreview() {
  return null;
}
```

Se tiver importadores ativos (alem de camera page), NAO alterar o arquivo e registrar como TODO no SUMMARY.

Depois rodar o build completo:

```bash
npm run build
```

Se o build passar, a fase esta completa.

Se houver erros de TypeScript ou lint no build:

- Ler os erros
- Corrigir cada um (tipicamente: import nao usado, tipo errado, prop faltando)
- Rodar build novamente
- Repetir ate passar
  </action>
  <verify>
  <automated>cd "/Users/luana/Documents/Code Projects/MaosFalam" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done> - npm run build passa sem erros - Nenhum erro de TypeScript no output do build - Nenhum erro de lint que bloqueie o build
  </done>
  </task>

</tasks>

<verification>
```bash
cd "/Users/luana/Documents/Code Projects/MaosFalam"
npm run build
```

Build passa. Fluxo verificavel manualmente em dev:

1. /ler/camera → method_choice → "Me mandar a foto"
2. UploadInstructionScreen aparece com outline SVG e dicas
3. "Escolher foto" abre file picker
4. Selecionar JPEG valido → UploadValidationScreen com checks progressivos
5. UploadConfirmScreen aparece com preview
6. Confirmar → /ler/scan
7. Selecionar GIF → check de formato falha com mensagem
   </verification>

<success_criteria>

- Fluxo multi-step completo funciona: instrucao → validacao → confirmacao
- GIF/SVG/BMP/PDF sao rejeitados com mensagem antes de qualquer processamento
- JPEG/PNG/WebP/HEIC aceitos e passam pelo pipeline de validacao
- "Usar mesmo assim" aparece quando mao detectada mas qualidade ruim
- Build passa sem erros
- Todos os 6 requirements UPL-01 a UPL-06 cobrertos
  </success_criteria>

<output>
Apos conclusao, criar `.planning/phases/02-upload-pipeline/02-03-SUMMARY.md`
</output>
