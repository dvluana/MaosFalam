---
phase: 19
plan: 01
name: Frontend Mao Mista
type: auto
autonomous: true
requirements: [MIX-07, MIX-08, MIX-09, MIX-10]
---

# Phase 19 Plan 01: Frontend Mao Mista

## Objective

Exibir secondary element de forma subordinada no resultado quando presente. Garantir backward compat para leituras antigas sem secondary_key.

## Context

- `src/types/report.ts` — `element.secondary_key?: HandElement` e `element.bridge?: string` sao opcionais
- `src/components/reading/ElementHero.tsx` — mostra elemento primario, recebe `element: { key: HandElement }`
- `src/components/reading/ElementSection.tsx` — card de elemento, recebe `element: ReportJSON["element"]`
- `src/components/reading/HandSummary.tsx` — exibe `portrait.exclusivity`, recebe `portrait: ReportJSON["portrait"]`

## Tasks

### Task 1: ElementHero — badge subordinado de secondary element

type: auto

Props de `ElementHero` ja recebem `element: { key: HandElement }`. Ampliar para aceitar `secondary_key?: HandElement` e exibir badge sutil "Com tracos de [Elemento]" abaixo do ornamental divider, antes do impact phrase. Null-guard completo: so renderiza quando `secondary_key` existe.

**Done criteria:**

- Badge renderiza com `secondary_key` presente
- Nada renderiza com `secondary_key` ausente (undefined)
- Texto: "Com tracos de Fogo" (etc) — maiuscula no nome do elemento
- Estilo: `font-jetbrains text-[10px] tracking-[1.5px] uppercase text-bone-dim`

### Task 2: ElementSection — bridge text apos body

type: auto

`ElementSection` ja recebe `element: ReportJSON["element"]` que inclui `bridge?: string`. Inserir `bridge` em paragraph `font-cormorant italic` apos o `<p>` de body e antes do impact phrase. Null-guard: so renderiza quando `bridge` existe e nao e string vazia.

**Done criteria:**

- Bridge paragraph aparece quando `element.bridge` existe
- Nada aparece quando `element.bridge` e undefined
- Estilo: `font-cormorant italic text-[17px] sm:text-[19px] text-bone leading-[1.45]` com `mt-4 mb-4`

### Task 3: HandSummary — props para secondary_element

type: auto

`HandSummary` recebe `portrait: ReportJSON["portrait"]` que ja contem `exclusivity`. O texto de exclusivity mista ja e injetado pelo motor (`ELEMENT_EXCLUSIVITY_MIXED`). Nao e necessario mudar logica — o texto correto ja chega no `portrait.exclusivity`. Verificar se ha algum hardcoding de `elementName` na frase que precisaria de ajuste, e adicionar `secondary_element?: HandElement` opcional nas props caso precise exibir algo extra. Se a exclusivity ja e suficiente (texto gerado pelo motor), apenas confirmar sem mudancas de codigo.

**Done criteria:**

- HandSummary compila sem erros
- Com leitura mista, `portrait.exclusivity` ja contem o texto correto do motor
- Se necessario algum ajuste no join de elementName, fazê-lo com optional chaining

### Task 4: Verificacao de backward compat + build final

type: auto

Confirmar que todas as mudancas usam optional chaining e sem slots vazios. Rodar `npm run lint && npm run type-check && npm run build && npm run test -- --run`.

**Done criteria:**

- Zero erros de lint
- Zero erros de type-check
- Build completo sem erros
- Testes passam

## Success Criteria

1. Leitura sem secondary_key renderiza sem crash e sem slots vazios
2. Leitura com secondary_key mostra "Com tracos de [Elemento]" em ElementHero
3. ElementSection exibe bridge apos body quando bridge existe
4. HandSummary usa exclusivity do motor (ja mista quando aplicavel)
