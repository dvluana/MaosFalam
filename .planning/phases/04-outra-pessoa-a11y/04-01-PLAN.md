---
phase: 04-outra-pessoa-a11y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/camera/HandInstructionOverlay.tsx
  - src/components/camera/HandExpectedBadge.tsx
  - src/components/camera/WrongHandFeedback.tsx
  - src/app/ler/camera/page.tsx
autonomous: true
requirements: [OTHER-01, OTHER-02]

must_haves:
  truths:
    - "Quando is_self=false, overlay da camera mostra o nome da outra pessoa na frase da cigana"
    - "Badge no viewfinder exibe 'MAO DIREITA . CARLOS' quando a leitura e pra outra pessoa"
    - "Toast de mao errada usa 'dela' para genero feminino e 'dele' para masculino quando is_self=false"
    - "Quando is_self=true, comportamento original e preservado sem regressao"
  artifacts:
    - path: src/components/camera/HandInstructionOverlay.tsx
      provides: "Props opcionais targetName e isSelf para adaptar texto da cigana"
      exports: [HandInstructionOverlay]
    - path: src/components/camera/HandExpectedBadge.tsx
      provides: "Prop targetName para adicionar nome ao badge quando is_self=false"
      exports: [HandExpectedBadge]
    - path: src/components/camera/WrongHandFeedback.tsx
      provides: "Props targetGender e isSelf para pronome correto no toast"
      exports: [WrongHandFeedback]
  key_links:
    - from: src/app/ler/camera/page.tsx
      to: src/components/camera/HandInstructionOverlay.tsx
      via: "props targetName={readingContext?.target_name} isSelf={readingContext?.is_self ?? true}"
      pattern: "HandInstructionOverlay"
    - from: src/components/camera/CameraViewport.tsx
      to: src/components/camera/HandExpectedBadge.tsx
      via: "prop targetName passado por CameraViewport"
      pattern: "HandExpectedBadge"
---

<objective>
Adaptar os componentes de camera ao contexto "leitura pra outra pessoa":
HandInstructionOverlay mostra o nome da pessoa na frase, HandExpectedBadge exibe
"MAO DIREITA . CARLOS", WrongHandFeedback usa pronome correto "dela"/"dele".

Purpose: Quando is_self=false, o usuario fotografa a mao de outra pessoa — todos os
textos da camera devem enderecar essa pessoa pelo nome e pronome correto.

Output: Tres componentes atualizados com props opcionais; camera page passando
readingContext para os componentes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/types/reading-context.ts
@src/lib/reading-context.ts
</context>

<interfaces>
<!-- ReadingContext — loaded in camera page via loadReadingContext() -->
```typescript
export interface ReadingContext {
  target_name: string;
  target_gender: "female" | "male";
  dominant_hand: "left" | "right";
  is_self: boolean;
  session_id: string;
  credit_used: boolean;
}
```

<!-- Current HandInstructionOverlay props -->

```typescript
interface HandInstructionOverlayProps {
  dominantHand: "right" | "left";
  onReady: () => void;
}
// phrase: "Me mostra a mao direita. Palma aberta, virada pra mim."
```

<!-- Current HandExpectedBadge props -->

```typescript
interface HandExpectedBadgeProps {
  dominantHand: "right" | "left";
}
// label: "MAO DIREITA" or "MAO ESQUERDA"
```

<!-- Current WrongHandFeedback props -->

```typescript
interface WrongHandFeedbackProps {
  expectedHand: "right" | "left";
  visible: boolean;
}
// text: "Essa e a outra mao. Me mostra a ${handLabel}."
```

<!-- CameraViewport — passes dominantHand to HandExpectedBadge -->
<!-- HandExpectedBadge rendered inside CameraViewport at line 86 -->

```typescript
interface Props {
  state: CamState;
  videoRef: React.RefObject<HTMLVideoElement | null>;
  canvasRef: React.RefObject<HTMLCanvasElement | null>;
  mirrored: boolean;
  dominantHand: "right" | "left";
  onSwitchCamera?: () => void;
}
```

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar props de contexto em HandInstructionOverlay, HandExpectedBadge e WrongHandFeedback</name>
  <files>
    src/components/camera/HandInstructionOverlay.tsx,
    src/components/camera/HandExpectedBadge.tsx,
    src/components/camera/WrongHandFeedback.tsx
  </files>
  <action>
    Leia cada arquivo antes de editar.

    **HandInstructionOverlay.tsx**

    Adicionar props opcionais `targetName?: string` e `isSelf?: boolean` (default `true`).

    Logica da frase (per OTHER-01):
    - `isSelf=true` (default): comportamento atual — "Me mostra a mao direita. Palma aberta, virada pra mim."
    - `isSelf=false` com `targetName`: "Me mostra a mao direita do {targetName}. Palma aberta, virada pra mim." (ou esquerda conforme dominantHand)

    **HandExpectedBadge.tsx**

    Adicionar prop opcional `targetName?: string`.

    Logica do label (per OTHER-01):
    - `targetName` ausente ou vazio: comportamento atual — "MAO DIREITA" ou "MAO ESQUERDA"
    - `targetName` presente: "MAO DIREITA . CARLOS" (uppercase, separado por " . ")
    - Format: `${handPart} . ${targetName.toUpperCase()}`

    Nota: O componente HandExpectedBadge e renderizado dentro de CameraViewport. CameraViewport recebe `dominantHand` hoje. Precisamos tambem passar `targetName` por CameraViewport para que chegue em HandExpectedBadge. Atualize `CameraViewport.tsx` (src/components/camera/CameraViewport.tsx) para aceitar prop opcional `targetName?: string` e repassar para HandExpectedBadge.

    **WrongHandFeedback.tsx**

    Adicionar props opcionais `isSelf?: boolean` (default `true`) e `targetGender?: "female" | "male"` (default `"female"`).

    Logica do texto (per OTHER-02):
    - `isSelf=true` (default): comportamento atual — "Essa e a outra mao. Me mostra a {handLabel}."
    - `isSelf=false`, `targetGender="female"`: "Essa e a mao errada dela. Me mostra a {handLabel} dela."
    - `isSelf=false`, `targetGender="male"`: "Essa e a mao errada dele. Me mostra a {handLabel} dele."

    Manter o mecanismo de rising-edge (prevVisibleRef, showCount) intacto — nao alterar a logica de animacao.

  </action>
  <verify>
    <automated>npm run type-check 2>&1 | head -30</automated>
  </verify>
  <done>
    Os tres componentes aceitam as novas props; type-check passa sem erros novos.
    HandExpectedBadge exibe nome quando targetName fornecido.
    CameraViewport aceita e repassa targetName.
  </done>
</task>

<task type="auto">
  <name>Task 2: Passar readingContext para os componentes na camera page</name>
  <files>
    src/app/ler/camera/page.tsx
  </files>
  <action>
    Leia o arquivo antes de editar.

    O arquivo ja carrega readingContext e extrai `dominantHand`:
    ```typescript
    const readingContext = loadReadingContext();
    const dominantHand = readingContext?.dominant_hand ?? "right";
    ```

    Adicionar extracoes adicionais logo abaixo (per OTHER-01, OTHER-02):
    ```typescript
    const targetName = readingContext?.target_name ?? "";
    const isSelf = readingContext?.is_self ?? true;
    const targetGender = readingContext?.target_gender ?? "female";
    ```

    Atualizar as tres chamadas de componente:

    1. `HandInstructionOverlay` (linha ~218):
    ```tsx
    <HandInstructionOverlay
      dominantHand={dominantHand}
      targetName={targetName}
      isSelf={isSelf}
      onReady={() => setState("loading_mediapipe")}
    />
    ```

    2. `CameraViewport` (linha ~301): adicionar prop `targetName`:
    ```tsx
    <CameraViewport
      state={state}
      videoRef={videoRef}
      canvasRef={canvasRef}
      mirrored={mirrored}
      dominantHand={dominantHand}
      targetName={targetName}
      onSwitchCamera={handleSwitchCamera}
    />
    ```

    3. `WrongHandFeedback` (linha ~332): adicionar props `isSelf` e `targetGender`:
    ```tsx
    <WrongHandFeedback
      expectedHand={dominantHand}
      visible={state === "camera_wrong_hand"}
      isSelf={isSelf}
      targetGender={targetGender}
    />
    ```

    Nao alterar nenhuma outra logica da pagina.

  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    Build passa sem erros. As props de contexto chegam nos tres componentes.
    Quando readingContext.is_self=false, os textos adaptam ao targetName e targetGender.
  </done>
</task>

</tasks>

<verification>
- `npm run type-check` sem erros novos
- `npm run build` passa
- Com is_self=false e target_name="Carlos", gender="male": badge exibe "MAO DIREITA . CARLOS", overlay diz "Me mostra a mao direita do Carlos", toast diz "...dele"
- Com is_self=true: comportamento original preservado
</verification>

<success_criteria>
OTHER-01: HandInstructionOverlay e HandExpectedBadge (via CameraViewport) mostram nome da outra pessoa quando is_self=false.
OTHER-02: WrongHandFeedback usa pronome "dela"/"dele" conforme target_gender quando is_self=false.
Build verde. Sem regressao no fluxo is_self=true.
</success_criteria>

<output>
Apos conclusao, crie `.planning/phases/04-outra-pessoa-a11y/04-01-SUMMARY.md`
</output>
