---
phase: 04-outra-pessoa-a11y
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/camera/UploadInstructionScreen.tsx
  - src/components/camera/UploadConfirmScreen.tsx
  - src/app/ler/camera/page.tsx
  - src/app/ler/nome/page.tsx
autonomous: true
requirements: [OTHER-03, A11Y-01, A11Y-02, A11Y-03]

must_haves:
  truths:
    - "UploadInstructionScreen menciona o nome da outra pessoa quando is_self=false"
    - "UploadConfirmScreen menciona o nome da outra pessoa no texto de confirmacao quando is_self=false"
    - "Botoes Destra/Canhota e Ela/Ele na pagina de nome tem aria-labels descritivos"
    - "aria-live assertive em WrongHandFeedback e polite em HandExpectedBadge (ja existem — confirmar sem regredir)"
    - "HandOutlineSVG tem role=img e aria-label (ja existe — confirmar sem regredir)"
  artifacts:
    - path: src/components/camera/UploadInstructionScreen.tsx
      provides: "Props opcionais targetName e isSelf para texto personalizado de upload"
      exports: [UploadInstructionScreen]
    - path: src/components/camera/UploadConfirmScreen.tsx
      provides: "Prop opcional targetName para confirmacao personalizada"
      exports: [UploadConfirmScreen]
    - path: src/app/ler/nome/page.tsx
      provides: "aria-labels em ToggleButton para leitores de tela"
  key_links:
    - from: src/app/ler/camera/page.tsx
      to: src/components/camera/UploadInstructionScreen.tsx
      via: "props targetName e isSelf passados quando showUpload=true"
      pattern: "UploadInstructionScreen"
    - from: src/app/ler/camera/page.tsx
      to: src/components/camera/UploadConfirmScreen.tsx
      via: "prop targetName passada quando uploadStep=confirm"
      pattern: "UploadConfirmScreen"
---

<objective>
Adaptar upload ao contexto "outra pessoa" e adicionar aria-labels nos toggles da pagina de nome.

Purpose: OTHER-03 — quando is_self=false, as telas de instrucao e confirmacao de upload
mencionam o nome da outra pessoa. A11Y-01 — os botoes Destra/Canhota e Ela/Ele precisam
de aria-labels para leitores de tela saberem o que cada botao representa.
A11Y-02 e A11Y-03 ja estao implementados — validar sem regredir.

Output: UploadInstructionScreen e UploadConfirmScreen com prop targetName;
ToggleButton com aria-label; camera page repassando contexto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/types/reading-context.ts
@src/lib/reading-context.ts
</context>

<interfaces>
<!-- ReadingContext -->
```typescript
export interface ReadingContext {
  target_name: string;
  target_gender: "female" | "male";
  dominant_hand: "left" | "right";
  is_self: boolean;
  session_id: string;
  credit_used: boolean;
}
```

<!-- Current UploadInstructionScreen props -->

```typescript
interface UploadInstructionScreenProps {
  dominantHand: "right" | "left";
  onContinue: () => void;
  onBack: () => void;
}
// handPhrase: "Fotografe sua mao direita. Palma aberta, virada pra cima."
```

<!-- Current UploadConfirmScreen props -->

```typescript
interface UploadConfirmScreenProps {
  result: ValidationResult;
  onConfirm: () => void;
  onRetry: () => void;
  onBack: () => void;
}
// Texto de sucesso: "Tudo certo. Essa mao fala."
// Texto qualidade ruim: "A mao esta aqui. A foto podia ser melhor."
```

<!-- ToggleButton in nome/page.tsx — sem aria-label hoje -->

```typescript
interface ToggleButtonProps {
  selected: boolean;
  onClick: () => void;
  children: React.ReactNode;
}
// <ToggleButton selected={dominantHand === "right"} onClick={...}>Destra</ToggleButton>
// <ToggleButton selected={dominantHand === "left"} onClick={...}>Canhota</ToggleButton>
// <ToggleButton selected={gender === "female"} onClick={...}>Ela</ToggleButton>
// <ToggleButton selected={gender === "male"} onClick={...}>Ele</ToggleButton>
```

<!-- A11Y-02: ja implementado — verificar sem alterar -->
<!-- WrongHandFeedback: aria-live="assertive" na div wrapper -->
<!-- HandExpectedBadge: aria-live="polite" na div wrapper -->

<!-- A11Y-03: ja implementado — verificar sem alterar -->
<!-- HandOutlineSVG: role="img" aria-label="Guia de posicionamento da mao direita/esquerda" -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Adaptar UploadInstructionScreen e UploadConfirmScreen ao contexto de outra pessoa</name>
  <files>
    src/components/camera/UploadInstructionScreen.tsx,
    src/components/camera/UploadConfirmScreen.tsx
  </files>
  <action>
    Leia cada arquivo antes de editar.

    **UploadInstructionScreen.tsx** (per OTHER-03)

    Adicionar props opcionais `targetName?: string` e `isSelf?: boolean` (default `true`).

    Logica da frase principal:
    - `isSelf=true` (default): comportamento atual — "Fotografe sua mao direita. Palma aberta, virada pra cima."
    - `isSelf=false` com `targetName`: "Fotografe a mao direita do {targetName}. Palma aberta, virada pra cima." (ou esquerda conforme dominantHand)

    Nao alterar os tips de qualidade (BOA LUZ, FUNDO LIMPO, DEDOS ABERTOS) — sao universais.

    **UploadConfirmScreen.tsx** (per OTHER-03)

    Adicionar prop opcional `targetName?: string`.

    Adaptar os textos do CASE A (hand ok + quality ok) e CASE B (hand ok, quality ruim):
    - `targetName` ausente: comportamento atual
      - CASE A: "Tudo certo. Essa mao fala."
      - CASE B: "A mao esta aqui. A foto podia ser melhor."
    - `targetName` presente:
      - CASE A: "Tudo certo. A mao do {targetName} fala."
      - CASE B: "A mao do {targetName} esta aqui. A foto podia ser melhor."

    CASE C (erro fatal) nao muda — e sobre o problema tecnico, nao sobre a pessoa.

    Nao alterar logica de validacao, checklist, ou botoes.

  </action>
  <verify>
    <automated>npm run type-check 2>&1 | head -30</automated>
  </verify>
  <done>
    Ambos os componentes aceitam as novas props sem erros de tipo.
    Comportamento padrao (sem targetName) preservado.
  </done>
</task>

<task type="auto">
  <name>Task 2: Passar targetName/isSelf no upload (camera page) e adicionar aria-labels nos toggles (nome page)</name>
  <files>
    src/app/ler/camera/page.tsx,
    src/app/ler/nome/page.tsx
  </files>
  <action>
    Leia cada arquivo antes de editar.

    **src/app/ler/camera/page.tsx** (per OTHER-03)

    O arquivo ja extrai `dominantHand` de readingContext. Se o plan 04-01 ja foi executado,
    `targetName` e `isSelf` ja estao extraidos — nao duplicar. Se este plan roda em paralelo
    (wave 1), adicionar as extraccoes se ainda nao existirem:
    ```typescript
    const targetName = readingContext?.target_name ?? "";
    const isSelf = readingContext?.is_self ?? true;
    ```

    Atualizar as chamadas dos componentes de upload:

    1. `UploadInstructionScreen` (dentro do bloco `uploadStep === "instruction"`):
    ```tsx
    <UploadInstructionScreen
      dominantHand={dominantHand}
      targetName={targetName}
      isSelf={isSelf}
      onContinue={() => uploadInputRef.current?.click()}
      onBack={handleUploadBack}
    />
    ```

    2. `UploadConfirmScreen` (dentro do bloco `uploadStep === "confirm"`):
    ```tsx
    <UploadConfirmScreen
      result={uploadResult}
      targetName={isSelf ? undefined : targetName}
      onConfirm={handleUploadConfirm}
      onRetry={handleUploadRetry}
      onBack={handleUploadBack}
    />
    ```

    Nao alterar nenhuma outra logica da camera page.

    ---

    **src/app/ler/nome/page.tsx** (per A11Y-01)

    A interface `ToggleButtonProps` nao tem `aria-label`. Adicionar prop opcional:
    ```typescript
    interface ToggleButtonProps {
      selected: boolean;
      onClick: () => void;
      children: React.ReactNode;
      ariaLabel?: string;
    }
    ```

    No `<button>` dentro de ToggleButton, adicionar:
    ```tsx
    aria-label={ariaLabel}
    aria-pressed={selected}
    ```

    `aria-pressed` e semanticamente correto para toggle buttons (indica estado ativo/inativo).

    Atualizar as chamadas com aria-labels descritivos:

    Toggles de dominancia (visitor flow e logged-in flow — ambos):
    ```tsx
    <ToggleButton
      selected={dominantHand === "right"}
      onClick={() => setDominantHand("right")}
      ariaLabel="Mao destra (direita)"
    >
      Destra
    </ToggleButton>
    <ToggleButton
      selected={dominantHand === "left"}
      onClick={() => setDominantHand("left")}
      ariaLabel="Mao canhota (esquerda)"
    >
      Canhota
    </ToggleButton>
    ```

    Toggles de genero (visitor flow e logged-in flow — ambos):
    ```tsx
    <ToggleButton
      selected={gender === "female"}
      onClick={() => setGender("female")}
      ariaLabel="Leitura para ela (feminino)"
    >
      Ela
    </ToggleButton>
    <ToggleButton
      selected={gender === "male"}
      onClick={() => setGender("male")}
      ariaLabel="Leitura para ele (masculino)"
    >
      Ele
    </ToggleButton>
    ```

    Toggles de Pra mim / Pra outra pessoa (logged-in flow):
    ```tsx
    <ToggleButton
      selected={isSelf}
      onClick={() => handleIsSelfToggle(true)}
      ariaLabel="Leitura para mim"
    >
      Pra mim
    </ToggleButton>
    <ToggleButton
      selected={!isSelf}
      onClick={() => handleIsSelfToggle(false)}
      ariaLabel="Leitura para outra pessoa"
    >
      Pra outra pessoa
    </ToggleButton>
    ```

    Nao alterar nenhuma logica de estado ou submit.

  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    Build passa sem erros.
    UploadInstructionScreen e UploadConfirmScreen recebem targetName/isSelf da camera page.
    ToggleButton na pagina de nome tem aria-label e aria-pressed em todos os toggles.
    A11Y-01, OTHER-03 satisfeitos.
  </done>
</task>

</tasks>

<verification>
- `npm run type-check` sem erros
- `npm run build` passa
- Com is_self=false e target_name="Maria": instrucao de upload diz "Fotografe a mao direita da Maria", confirmacao diz "A mao da Maria fala"
- Inspecionar DOM de /ler/nome: botoes Destra/Canhota tem aria-label e aria-pressed
- A11Y-02: WrongHandFeedback tem aria-live="assertive" (nao regrediu)
- A11Y-02: HandExpectedBadge tem aria-live="polite" (nao regrediu)
- A11Y-03: HandOutlineSVG tem role="img" e aria-label (nao regrediu)
</verification>

<success_criteria>
OTHER-03: Textos de upload personalizam para nome da outra pessoa quando is_self=false.
A11Y-01: Todos os ToggleButtons em /ler/nome tem aria-label e aria-pressed.
A11Y-02 e A11Y-03: Confirmados sem regressao.
Build verde.
</success_criteria>

<output>
Apos conclusao, crie `.planning/phases/04-outra-pessoa-a11y/04-02-SUMMARY.md`
</output>
