---
phase: 04-clerk-cleanup-error-handling
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/esqueci-senha/page.tsx
  - src/app/redefinir-senha/[token]/page.tsx
  - src/app/conta/perfil/page.tsx
autonomous: true
requirements: [CLK-01, CLK-02, CLK-03, CLK-04]

must_haves:
  truths:
    - "Acessar /esqueci-senha redireciona imediatamente para /login sem renderizar formulario"
    - "Acessar /redefinir-senha/qualquer-token redireciona imediatamente para /login sem renderizar formulario"
    - "/conta/perfil nao tem formulario manual de edicao de nome nem de troca de senha"
    - "/conta/perfil renderiza o componente Clerk UserProfile para que o usuario edite nome e senha"
    - "Botao Sair e confirmacao de logout continuam funcionando em /conta/perfil"
  artifacts:
    - path: src/app/esqueci-senha/page.tsx
      provides: "Redirect para /login; zero formulario"
      contains: "redirect"
    - path: src/app/redefinir-senha/[token]/page.tsx
      provides: "Redirect para /login; zero formulario"
      contains: "redirect"
    - path: src/app/conta/perfil/page.tsx
      provides: "UserProfile do Clerk + logout"
      contains: "UserProfile"
  key_links:
    - from: src/app/esqueci-senha/page.tsx
      to: /login
      via: "Next.js redirect() ou useRouter().replace()"
      pattern: "redirect.*login"
    - from: src/app/conta/perfil/page.tsx
      to: "@clerk/nextjs UserProfile"
      via: "import { UserProfile } from '@clerk/nextjs'"
      pattern: "UserProfile"
---

<objective>
Delegar os fluxos de esqueci-senha e redefinir-senha ao Clerk (que ja gerencia esses flows via SignIn com hash routing) e substituir os formularios manuais de perfil pelo componente Clerk UserProfile.

Purpose: Clerk e o source of truth de auth — formularios customizados sao duplicacao de logica que o Clerk ja fornece e testa. Manter os forms custom cria superficie de bugs sem beneficio.
Output: Tres arquivos reescritos. Zero formularios manuais de auth.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Clerk components disponiveis em @clerk/nextjs -->
<!-- Padrao existente em src/app/login/page.tsx: -->

```typescript
import { SignIn } from "@clerk/nextjs";
import { dark } from "@clerk/themes";

<SignIn
  appearance={{
    baseTheme: dark,
    variables: {
      colorPrimary: "#C9A24A",
      colorBackground: "#110C1A",
      colorText: "#E8DFD0",
      colorTextSecondary: "#9b9284",
      colorInputBackground: "#171222",
      colorInputText: "#E8DFD0",
      borderRadius: "0.375rem",
    },
  }}
  routing="hash"
  signUpUrl="/registro"
  fallbackRedirectUrl="/conta/leituras"
/>
```

<!-- UserProfile component: -->

```typescript
import { UserProfile } from "@clerk/nextjs";

<UserProfile
  appearance={{
    baseTheme: dark,
    variables: { ... }
  }}
/>
```

<!-- Next.js redirect (server component): -->

```typescript
import { redirect } from "next/navigation";
export default function Page() {
  redirect("/login");
}
```

<!-- useRouter redirect (client component): -->

```typescript
"use client";
import { useEffect } from "react";
import { useRouter } from "next/navigation";
export default function Page() {
  const router = useRouter();
  useEffect(() => {
    router.replace("/login");
  }, [router]);
  return null;
}
```

<!-- useAuth hook (src/hooks/useAuth.ts) — expoe: -->

```typescript
export function useAuth(): {
  user: { name: string; email: string } | null;
  logout: () => void;
};
```

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Redirect /esqueci-senha e /redefinir-senha para /login</name>
  <files>src/app/esqueci-senha/page.tsx, src/app/redefinir-senha/[token]/page.tsx</files>
  <action>
    Reescrever ambas as paginas como server components que redirecionam imediatamente para /login. Clerk gerencia todo o fluxo de recuperacao de senha via SignIn com hash routing — nao existe motivo para manter form customizado.

    Para src/app/esqueci-senha/page.tsx:
    - Remover toda logica de formulario, estados, imports de Button/Input/Card/StateSwitcher
    - Tornar server component (sem "use client")
    - Chamar `redirect("/login")` do `next/navigation`
    - Nao adicionar nenhum JSX nem texto ao usuario — apenas redirect

    Para src/app/redefinir-senha/[token]/page.tsx:
    - Remover toda logica de formulario e validacao de token
    - Tornar server component (sem "use client") com async params
    - Chamar `redirect("/login")` independente do token recebido
    - Nao adicionar nenhum JSX — apenas redirect

    Padrao de server component com redirect (per CLK-01, CLK-02):
    ```typescript
    import { redirect } from "next/navigation";

    export default function EsqueciSenhaPage() {
      redirect("/login");
    }
    ```

  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    GET /esqueci-senha retorna 307 redirect para /login.
    GET /redefinir-senha/qualquer-token retorna 307 redirect para /login.
    npm run build passa sem erros de tipo.
  </done>
</task>

<task type="auto">
  <name>Task 2: Substituir formularios manuais de perfil por Clerk UserProfile</name>
  <files>src/app/conta/perfil/page.tsx</files>
  <action>
    Reescrever /conta/perfil para usar o componente UserProfile do Clerk em vez dos formularios manuais de edicao de nome e troca de senha (per CLK-03, CLK-04).

    Estrutura da nova pagina:
    - Manter "use client" (UserProfile e componente cliente)
    - Manter o header visual existente ("Sua conta" + "Quem voce e pra mim.")
    - Manter o bloco de logout com confirmacao (botao "Sair" + "Tem certeza?" + "Sim, sair" / "Cancelar") — esse comportamento nao e do Clerk, usar useAuth().logout() + router.push("/")
    - Remover: states Mode, nameInput, currentPwd, newPwd, confirmPwd, pwdError, pwdSuccess, Card de dados basicos com formulario de nome, Card de trocar senha com formulario de senha
    - Adicionar: `<UserProfile>` do Clerk com o mesmo tema dark/gold do login page

    Aparencia do UserProfile (usar variaveis identicas ao SignIn do login):
    ```typescript
    import { UserProfile } from "@clerk/nextjs";
    import { dark } from "@clerk/themes";

    <UserProfile
      appearance={{
        baseTheme: dark,
        variables: {
          colorPrimary: "#C9A24A",
          colorBackground: "#110C1A",
          colorText: "#E8DFD0",
          colorTextSecondary: "#9b9284",
          colorInputBackground: "#171222",
          colorInputText: "#E8DFD0",
          borderRadius: "0.375rem",
        },
      }}
    />
    ```

    Layout final da pagina (de cima pra baixo):
    1. Header com badge "Sua conta" e titulo "Quem voce e pra mim."
    2. UserProfile component (preenche a secao de edicao)
    3. Separator variant="gold"
    4. Bloco de logout com confirmacao (mantido igual ao atual)

    Remover imports que nao sao mais necessarios: useState modos, Input, Card de dados, pwdError etc.
    Manter: useRouter, useAuth, Button, Separator.

    Nao usar StateSwitcher nessa pagina (nao havia antes; nao adicionar).

  </action>
  <verify>
    <automated>npm run type-check 2>&1 | grep -E "error|Error" | head -10; echo "exit:$?"</automated>
  </verify>
  <done>
    /conta/perfil renderiza UserProfile do Clerk sem nenhum formulario manual de nome ou senha.
    Botao "Sair" com confirmacao continua funcionando.
    npm run type-check passa sem erros.
  </done>
</task>

</tasks>

<verification>
npm run build
Sem erros de tipo. Sem referencias a formularios de senha customizados em esqueci-senha, redefinir-senha, ou conta/perfil.
</verification>

<success_criteria>

- GET /esqueci-senha -> redirect 307 /login (per CLK-01)
- GET /redefinir-senha/[token] -> redirect 307 /login (per CLK-02)
- /conta/perfil renderiza UserProfile do Clerk, sem form de nome e sem form de senha (per CLK-03, CLK-04)
- npm run build passa
  </success_criteria>

<output>
Apos conclusao, criar `.planning/phases/04-clerk-cleanup-error-handling/04-01-SUMMARY.md`
</output>
