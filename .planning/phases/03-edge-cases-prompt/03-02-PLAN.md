---
phase: 03-edge-cases-prompt
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useLandscapeGuard.ts
  - src/hooks/useFailureCounter.ts
  - src/app/ler/camera/page.tsx
  - src/components/camera/LandscapeWarning.tsx
  - src/hooks/useUploadValidation.ts
autonomous: true
requirements: [EDGE-04, EDGE-05, EDGE-06]

must_haves:
  truths:
    - "Celular em landscape exibe aviso em tela cheia 'Vira o celular pra vertical' e nao prossegue"
    - "Apos 3 falhas (camera ou upload), usuario ve sugestao de trocar de metodo"
    - "Screenshot detectado por dimensoes atipicas exibe aviso da cigana pedindo foto real"
  artifacts:
    - path: "src/hooks/useLandscapeGuard.ts"
      provides: "hook que retorna isLandscape boolean baseado em window.innerWidth > window.innerHeight"
      exports: ["useLandscapeGuard"]
    - path: "src/components/camera/LandscapeWarning.tsx"
      provides: "tela de aviso fullscreen quando isLandscape=true"
    - path: "src/hooks/useFailureCounter.ts"
      provides: "hook que conta falhas e retorna suggestMethodSwitch quando >= 3"
      exports: ["useFailureCounter"]
    - path: "src/hooks/useUploadValidation.ts"
      provides: "detect screenshot por dimensoes atipicas, retorna erro especifico"
  key_links:
    - from: "src/app/ler/camera/page.tsx"
      to: "src/hooks/useLandscapeGuard.ts"
      via: "useLandscapeGuard(); render LandscapeWarning when isLandscape"
      pattern: "useLandscapeGuard"
    - from: "src/app/ler/camera/page.tsx"
      to: "src/hooks/useFailureCounter.ts"
      via: "useFailureCounter(); pass onFailure to handlers; show switch suggestion"
      pattern: "useFailureCounter"
    - from: "src/hooks/useUploadValidation.ts"
      to: "screenshot detection"
      via: "check image dimensions vs known screenshot aspect ratios in CHECK 3 (after format check)"
      pattern: "screenshot"
---

<objective>
Implementar deteccao de landscape, retry logic, e deteccao de screenshot.

Purpose: Celular em landscape quebra a camera e o outline. Retry excessivo frustra a usuario. Screenshot enviado como foto de palma vai desperdicar uma chamada GPT-4o com confidence baixissima.
Output: hook useLandscapeGuard + LandscapeWarning + hook useFailureCounter + screenshot detection em useUploadValidation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-upload-pipeline/02-03-SUMMARY.md

<interfaces>
<!-- Contratos que o executor precisa -->

From src/app/ler/camera/page.tsx (estrutura atual):

```typescript
// Estado gerenciado localmente com useState<CamState>
// showUpload: boolean para toggle do fluxo de upload
// uploadStep: "instruction" | "validating" | "confirm"
// Handlers existentes: handleUploadRetry, handleCaptured, handleUploadConfirm
// Nenhuma contagem de falhas hoje — o executor deve adicionar

// Render pattern: tela inteira e <main> com posicao relative
// LandscapeWarning deve ser rendered ANTES de tudo — retorna fullscreen overlay
```

From src/hooks/useUploadValidation.ts (resultado dos checks):

```typescript
export interface ValidationResult {
  phase: "idle" | "validating" | "done";
  checks: ValidationCheck[];
  previewUrl: string | null;
  file: File | null;
  handOk: boolean;
  qualityOk: boolean;
  canProceed: boolean;
  error: string | null;
}
// O check "format" e o CHECK 1 — screenshot deve ser verificado APOS normalizeImage
// mas ANTES do MediaPipe, como parte do CHECK 1 ou novo sub-check
// Alternativa mais simples: adicionar ao error do CHECK 1 se dimensoes sao atipicas
```

From src/types/camera.ts (CamState ja existente):

```typescript
// Nao adicionar novos estados — usar estado existente e flag adicional
// O suggestMethodSwitch e flag booleana, nao um estado da camera
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useLandscapeGuard + LandscapeWarning</name>
  <files>src/hooks/useLandscapeGuard.ts, src/components/camera/LandscapeWarning.tsx</files>
  <action>
**src/hooks/useLandscapeGuard.ts:**

Hook simples que detecta orientacao landscape e escuta mudancas:

```typescript
"use client";
import { useEffect, useState } from "react";

export function useLandscapeGuard(): boolean {
  const [isLandscape, setIsLandscape] = useState(false);

  useEffect(() => {
    function check() {
      setIsLandscape(window.innerWidth > window.innerHeight);
    }
    check();
    window.addEventListener("resize", check);
    window.addEventListener("orientationchange", check);
    return () => {
      window.removeEventListener("resize", check);
      window.removeEventListener("orientationchange", check);
    };
  }, []);

  return isLandscape;
}
```

Tambem tentar `screen.orientation?.lock?.("portrait").catch(() => {})` no useEffect de setup — silencioso se nao suportado.

**src/components/camera/LandscapeWarning.tsx:**

Componente fullscreen que aparece quando isLandscape=true. Design seguindo o DS:

- Fundo: `#08050E` (black token)
- Frase da cigana em Cormorant Garamond italic: `"Vira o celular pra vertical."` — cor bone, centralizada
- Sub-texto em Raleway 13px bone-dim: `"Preciso ver sua mao com clareza."`
- Nenhum botao — desaparece automaticamente quando vira o celular
- Sem emojis. Sem icone de rotacao (marca nao usa).
- Posicao: `fixed inset-0 z-50` pra cobrir tudo

Props: `{ visible: boolean }` — usar AnimatePresence + motion.div com fade simples (0.3s).

**Integracao em camera/page.tsx:**

- Importar `useLandscapeGuard` e `LandscapeWarning`
- Chamar hook: `const isLandscape = useLandscapeGuard();`
- Renderizar no inicio do JSX (antes de tudo no return): `<LandscapeWarning visible={isLandscape} />`
- O warning cobre a tela quando landscape sem bloquear o estado interno

**LEIA o arquivo camera/page.tsx antes de editar.** Manter a ordem de imports existente (ESLint import/order).
</action>
<verify>
<automated>cd "/Users/luana/Documents/Code Projects/MaosFalam" && npm run type-check 2>&1 | tail -5</automated>
</verify>
<done>useLandscapeGuard.ts e LandscapeWarning.tsx existem; camera/page.tsx usa o hook e renderiza o warning; type-check passa.</done>
</task>

<task type="auto">
  <name>Task 2: useFailureCounter + screenshot detection</name>
  <files>src/hooks/useFailureCounter.ts, src/hooks/useUploadValidation.ts, src/app/ler/camera/page.tsx</files>
  <action>
**src/hooks/useFailureCounter.ts:**

Hook que conta falhas e sinaliza pra sugerir troca de metodo apos 3:

```typescript
"use client";
import { useCallback, useState } from "react";

export interface FailureCounterResult {
  failureCount: number;
  suggestMethodSwitch: boolean; // true quando failureCount >= 3
  recordFailure: () => void;
  resetFailures: () => void;
}

export function useFailureCounter(): FailureCounterResult {
  const [failureCount, setFailureCount] = useState(0);
  const recordFailure = useCallback(() => setFailureCount((n) => n + 1), []);
  const resetFailures = useCallback(() => setFailureCount(0), []);
  return {
    failureCount,
    suggestMethodSwitch: failureCount >= 3,
    recordFailure,
    resetFailures,
  };
}
```

**Integracao em camera/page.tsx:**

Ler o arquivo antes de editar.

1. Importar `useFailureCounter` e chamar: `const { suggestMethodSwitch, recordFailure, resetFailures } = useFailureCounter();`

2. Chamar `recordFailure()` nos seguintes handlers existentes:
   - No final de `handleUploadRetry` (usuario clicou retry = uma falha)
   - Quando `uploadResult.canProceed === false` e o usuario esta na tela de confirm (monitorar via useEffect ou no proprio handleUploadRetry)
   - Na tela de camera: quando `state` entra em `camera_error_generic` (via useEffect)

3. Chamar `resetFailures()` quando:
   - `handleCaptured` e chamado com sucesso (camera capturou)
   - `handleUploadConfirm` e chamado com sucesso (upload confirmado)

4. Quando `suggestMethodSwitch === true`, exibir sugestao na UI:
   - Se `showUpload === false` (usuario ta na camera): mostrar mensagem na area de feedback da cigana "Tente agora na galeria — uma foto bem iluminada resolve."
   - Se `showUpload === true` (usuario ta no upload): mostrar mensagem "Tente agora pela camera — ao vivo fica mais facil."
   - A mensagem aparece como texto adicional abaixo do feedback normal, em Cormorant italic bone-dim, pequeno (15px). Nao e um toast separado — e inline.
   - Manter a logica existente de `CameraFeedback` intacta; adicionar o texto de sugestao como elemento separado renderizado logo abaixo.

**Deteccao de screenshot em useUploadValidation.ts:**

Ler o arquivo antes de editar.

Adicionar deteccao de screenshot como parte do CHECK 1 (format), APOS a verificacao de HEIC/tipo/tamanho mas ANTES dos outros checks. Inserir logo antes de `updateCheck("size", { status: "running" })`:

```typescript
// Screenshot detection: check image dimensions against known screenshot aspect ratios
// Common iPhone screenshot: 1170x2532, 1284x2778, 828x1792
// Detection: if width is an exact match to known device width AND height >> width (>1.8 ratio)
// More robust: load image, check if dimensions match phone screenshot patterns exactly
const screenshotDetected = await checkIfScreenshot(normalizedFile);
if (screenshotDetected) {
  updateCheck("format", { status: "fail" });
  setResult((prev) => ({
    ...prev,
    phase: "done",
    error: "Isso parece um print, nao uma foto. Preciso ver sua mao de verdade.",
  }));
  return;
}
```

Criar funcao auxiliar `checkIfScreenshot(file: File): Promise<boolean>` no mesmo arquivo (nao exportar — local):

```typescript
// Known iPhone/Android screenshot widths (logical pixels, common models 2022+)
const SCREENSHOT_WIDTHS = new Set([1170, 1179, 1284, 1290, 1080, 828, 750, 1242]);

async function checkIfScreenshot(file: File): Promise<boolean> {
  return new Promise((resolve) => {
    const img = new Image();
    const url = URL.createObjectURL(file);
    img.onload = () => {
      URL.revokeObjectURL(url);
      const { naturalWidth: w, naturalHeight: h } = img;
      // Typical screenshot: tall aspect ratio AND exact device pixel width
      const aspectRatio = h / w;
      resolve(SCREENSHOT_WIDTHS.has(w) && aspectRatio > 1.8);
    };
    img.onerror = () => {
      URL.revokeObjectURL(url);
      resolve(false);
    };
    img.src = url;
  });
}
```

Posicionar o bloco `checkIfScreenshot` logo apos o CHECK 1 de formato (depois do `updateCheck("format", { status: "pass", detail: typeLabel })`), antes do CHECK 2 de tamanho. Nao criar um novo `ValidationCheck` — usar o error do estado existente.

**LEIA cada arquivo antes de editar.** Manter ordem de imports existente para nao violar ESLint.
</action>
<verify>
<automated>cd "/Users/luana/Documents/Code Projects/MaosFalam" && npm run lint 2>&1 | tail -10 && npm run type-check 2>&1 | tail -5</automated>
</verify>
<done>useFailureCounter.ts existe e exporta hook; camera/page.tsx chama recordFailure nas falhas e exibe sugestao quando suggestMethodSwitch=true; useUploadValidation detecta screenshots por dimensoes e retorna erro especifico; lint e type-check passam.</done>
</task>

</tasks>

<verification>
- useLandscapeGuard.ts e LandscapeWarning.tsx existem
- camera/page.tsx renderiza LandscapeWarning quando isLandscape=true
- useFailureCounter.ts existe com recordFailure/resetFailures/suggestMethodSwitch
- camera/page.tsx usa useFailureCounter e exibe sugestao apos 3 falhas
- useUploadValidation detecta screenshots com SCREENSHOT_WIDTHS + aspect ratio
- npm run lint passa
- npm run type-check passa
- npm run build passa
</verification>

<success_criteria>

- EDGE-04: Celular em landscape exibe aviso "Vira o celular pra vertical" sem prosseguir
- EDGE-05: Apos 3 falhas, usuario ve sugestao de trocar de metodo (camera <> upload)
- EDGE-06: Screenshot detectado por dimensoes atipicas exibe aviso da cigana pedindo foto real
  </success_criteria>

<output>
Apos conclusao, criar `.planning/phases/03-edge-cases-prompt/03-02-SUMMARY.md`
</output>
