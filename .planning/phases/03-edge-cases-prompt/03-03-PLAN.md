---
phase: 03-edge-cases-prompt
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/lib/openai.ts
  - src/app/api/reading/capture/route.ts
autonomous: true
requirements: [PROMPT-01, PROMPT-02]

must_haves:
  truths:
    - "O prompt enviado ao GPT-4o menciona explicitamente qual mao dominante esta sendo analisada"
    - "O prompt instrui GPT-4o a ignorar tatuagens, henna, nail art, aneis, pulseiras e acessorios"
  artifacts:
    - path: "src/server/lib/openai.ts"
      provides: "analyzeHand aceita dominantHand param e injeta contexto no prompt"
      exports: ["analyzeHand"]
    - path: "src/app/api/reading/capture/route.ts"
      provides: "passa dominant_hand no payload validado ao analyzeHand"
  key_links:
    - from: "src/app/api/reading/capture/route.ts"
      to: "src/server/lib/openai.ts"
      via: "analyzeHand(data.photo_base64, data.dominant_hand)"
      pattern: "dominant_hand"
    - from: "src/server/lib/openai.ts"
      to: "OpenAI API"
      via: "PROMPT string interpolation com dominantHand"
      pattern: "mao dominante"
---

<objective>
Atualizar o prompt do GPT-4o pra incluir contexto de mao dominante e instrucao pra ignorar acessorios.

Purpose: Sem saber qual mao esta sendo analisada, o GPT-4o pode interpretar orientacao errada. Henna/tatuagens podem ser confundidas com linhas quiromanticas, reduzindo a precisao do confidence.
Output: analyzeHand() recebe dominantHand como parametro obrigatorio; PROMPT contem as adicoes de maodominante.md secao 2.9; rota de captura valida e passa dominant_hand.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Contratos existentes que serao modificados -->

From src/server/lib/openai.ts (assinatura atual):

```typescript
export async function analyzeHand(photoBase64: string): Promise<HandAttributes>;
// Sera alterada para:
export async function analyzeHand(
  photoBase64: string,
  dominantHand: "right" | "left",
): Promise<HandAttributes>;
```

From src/app/api/reading/capture/route.ts (schema Zod atual):

```typescript
const schema = z.object({
  photo_base64: z.string().min(100),
  session_id: z.string().min(10).max(64),
  lead_id: z.string().uuid().optional(),
  target_name: z.string().min(2).max(100),
  target_gender: z.enum(["female", "male"]),
  is_self: z.boolean(),
  // adicionar: dominant_hand: z.enum(["right", "left"])
});

// Chamada atual:
const attributes = await analyzeHand(data.photo_base64);
// Sera:
const attributes = await analyzeHand(data.photo_base64, data.dominant_hand);
```

From src/lib/reading-client.ts (adapter front-end):
// Verificar se o payload de captura precisa ser atualizado — leia o arquivo
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar analyzeHand e PROMPT em openai.ts</name>
  <files>src/server/lib/openai.ts</files>
  <action>
Ler o arquivo antes de editar.

**Mudancas em src/server/lib/openai.ts:**

1. Alterar a assinatura de `analyzeHand`:

```typescript
export async function analyzeHand(
  photoBase64: string,
  dominantHand: "right" | "left",
): Promise<HandAttributes>;
```

2. No corpo da funcao, antes de chamar fetch, construir o texto de contexto:

```typescript
const handLabel = dominantHand === "right" ? "direita" : "esquerda";
const dominanceContext = `Esta e a mao ${handLabel} da pessoa. E a mao dominante (a que ela escreve com). Analise considerando a orientacao correta da palma. Ignore tatuagens, henna, nail art, aneis, pulseiras e qualquer acessorio visivel. Analise APENAS as linhas naturais da palma, montes, e sinais quiromanticos.`;
```

3. Injetar esse contexto no `content` da mensagem do user, como primeiro item antes da imagem:

```typescript
{
  role: "user",
  content: [
    { type: "text", text: dominanceContext },
    { type: "text", text: "Analise esta palma." },
    {
      type: "image_url",
      image_url: { url: `data:image/jpeg;base64,${photoBase64}`, detail: "high" },
    },
  ],
}
```

Alternativa igualmente valida: adicionar `dominanceContext` como ultimo paragrafo do PROMPT constante via interpolacao. A abordagem de dois text blocks no user message e preferida porque: (a) o system prompt fica estatico pra cache do OpenAI, (b) contexto dinamico vai no user turn.

**Manter:**

- Toda a logica de Zod parse e validacao existente
- `logger.info` e `logger.warn` existentes (nunca logar photoBase64 ou dominantHand completo)
- `HAND_ATTRIBUTES_SCHEMA` e `HandAttributesSchema` sem mudancas
- `max_tokens: 1500` e `response_format` sem mudancas

**Sem `any`.** O `content` do user message e tipado como array de objetos — verificar se o tipo atual suporta multiplos text blocks; se nao, usar `as Parameters<typeof fetch>[1]["body"]` cast minimo necessario ou verificar tipagem do fetch body.
</action>
<verify>
<automated>cd "/Users/luana/Documents/Code Projects/MaosFalam" && npm run type-check 2>&1 | tail -5</automated>
</verify>
<done>analyzeHand aceita dominantHand como segundo parametro obrigatorio; dominanceContext e injetado na mensagem do user; type-check passa.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar rota de captura e adapter front-end</name>
  <files>src/app/api/reading/capture/route.ts, src/lib/reading-client.ts</files>
  <action>
Ler ambos os arquivos antes de editar.

**src/app/api/reading/capture/route.ts:**

1. Adicionar `dominant_hand` ao schema Zod:

```typescript
const schema = z.object({
  photo_base64: z.string().min(100),
  session_id: z.string().min(10).max(64),
  lead_id: z.string().uuid().optional(),
  target_name: z.string().min(2).max(100),
  target_gender: z.enum(["female", "male"]),
  is_self: z.boolean(),
  dominant_hand: z.enum(["right", "left"]).default("right"),
});
```

O `.default("right")` garante retrocompatibilidade — clientes antigos sem o campo continuam funcionando.

2. Atualizar a chamada a `analyzeHand`:

```typescript
const attributes = await analyzeHand(data.photo_base64, data.dominant_hand);
```

**src/lib/reading-client.ts:**

Verificar a assinatura de `captureReading` (ou funcao equivalente que chama a rota). Se ela monta o payload sem `dominant_hand`, adicionar o campo:

- Localizar onde `maosfalam_photo` e lido do sessionStorage e enviado via fetch/POST
- Adicionar leitura de `dominant_hand` do ReadingContext: `loadReadingContext()?.dominant_hand ?? "right"`
- Incluir no payload: `dominant_hand: dominantHand`

Se reading-client.ts nao existir ou a funcao nao existir, verificar onde o POST pra `/api/reading/capture` e feito no front-end e atualizar la. Grep por `reading/capture` se necessario.

**Sem `any`.** Tipar o payload de fetch corretamente.
</action>
<verify>
<automated>cd "/Users/luana/Documents/Code Projects/MaosFalam" && npm run lint 2>&1 | tail -10 && npm run type-check 2>&1 | tail -5 && npm run build 2>&1 | tail -10</automated>
</verify>
<done>Schema Zod aceita dominant_hand com default "right"; analyzeHand chamado com data.dominant_hand; adapter front-end passa dominant_hand no payload; lint + type-check + build passam.</done>
</task>

</tasks>

<verification>
- analyzeHand(photoBase64, dominantHand) e a nova assinatura
- PROMPT ou user content inclui "Esta e a mao [direita/esquerda] da pessoa. E a mao dominante."
- PROMPT inclui instrucao de ignorar tatuagens, henna, nail art, aneis, pulseiras
- Schema Zod da rota aceita dominant_hand com default "right"
- Adapter front-end passa dominant_hand
- npm run lint passa
- npm run type-check passa
- npm run build passa
</verification>

<success_criteria>

- PROMPT-01: Prompt do GPT-4o inclui qual mao dominante esta sendo analisada
- PROMPT-02: Prompt instrui GPT-4o a ignorar tatuagens, henna, nail art, aneis, pulseiras e acessorios
  </success_criteria>

<output>
Apos conclusao, criar `.planning/phases/03-edge-cases-prompt/03-03-SUMMARY.md`
</output>
