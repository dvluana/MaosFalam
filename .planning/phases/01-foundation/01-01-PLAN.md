---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma.config.ts
  - src/server/lib/logger.ts
  - .env.example
autonomous: false
requirements: [DB-01, DB-02, DB-03, INFRA-01, INFRA-02, INFRA-03, SEC-07]

must_haves:
  truths:
    - 'prisma/schema.prisma has url = env("DATABASE_URL") and directUrl = env("DIRECT_URL") in its datasource block'
    - "prisma.config.ts uses DIRECT_URL (not DATABASE_URL) so CLI migrations bypass PgBouncer"
    - "Pino logger has redact config covering name, email, cpf, phone at root and nested paths"
    - ".env.example documents both DATABASE_URL and DIRECT_URL with comments"
    - ".env.local (or .env) has real Neon connection strings in both DATABASE_URL and DIRECT_URL"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "PostgreSQL datasource with two-URL pattern"
      contains: "directUrl"
    - path: "prisma.config.ts"
      provides: "Prisma CLI override to use DIRECT_URL"
      contains: "DIRECT_URL"
    - path: "src/server/lib/logger.ts"
      provides: "PII-safe structured logger"
      contains: "redact"
    - path: ".env.example"
      provides: "Environment documentation"
      contains: "DIRECT_URL"
  key_links:
    - from: "prisma.config.ts"
      to: 'process.env["DIRECT_URL"]'
      via: "datasource.url override"
      pattern: "DIRECT_URL"
    - from: "src/server/lib/logger.ts"
      to: "pino redact config"
      via: "redact.paths array"
      pattern: "redact"
---

<objective>
Fix the four infrastructure gaps that block every downstream phase: Neon database provisioning, Prisma two-URL config, Pino PII redaction, and env documentation.

Purpose: Phase 1 cannot proceed until these are in place. Without DIRECT_URL in prisma.config.ts, migrations fail. Without redact in logger.ts, SEC-07 is violated before any API routes ship.
Output: Neon project created, all four files corrected, .env.local populated with real connection strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Provision Neon database and collect connection strings</name>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md (sections: Environment Availability, Pitfall 5)
  </read_first>
  <action>
    Use the Neon MCP tool to create the project and retrieve both connection strings.

    Run in order:
    1. mcp__Neon__create_project with name "maosfalam"
    2. mcp__Neon__get_connection_string for the pooled URL (default branch, pooled=true) — this is DATABASE_URL
    3. mcp__Neon__get_connection_string for the direct URL (default branch, pooled=false) — this is DIRECT_URL

    Then create/update .env.local with:
    ```
    DATABASE_URL=<pooled connection string from step 2>
    DIRECT_URL=<direct connection string from step 3>
    ```

    The pooled hostname contains "-pooler" (e.g., ep-xxx.pooler.neon.tech).
    The direct hostname does NOT contain "-pooler" (e.g., ep-xxx.neon.tech).

    Both URLs must end with ?sslmode=require.

  </action>
  <how-to-verify>
    After running MCP tools, confirm:
    1. Two distinct connection strings returned (one with "-pooler" in hostname, one without)
    2. .env.local exists and contains both DATABASE_URL and DIRECT_URL with real values (not empty)
    3. Run: grep "DATABASE_URL\|DIRECT_URL" .env.local — both lines must show non-empty values
  </how-to-verify>
  <resume-signal>Type "provisioned" once .env.local has both DATABASE_URL and DIRECT_URL filled in with real Neon connection strings</resume-signal>
  <acceptance_criteria>
    - .env.local contains DATABASE_URL with a non-empty postgresql:// value containing "pooler"
    - .env.local contains DIRECT_URL with a non-empty postgresql:// value WITHOUT "pooler"
    - Both URLs end with ?sslmode=require
    - grep "DATABASE_URL" .env.local returns a line with a value (not just "DATABASE_URL=")
    - grep "DIRECT_URL" .env.local returns a line with a value (not just "DIRECT_URL=")
  </acceptance_criteria>
  <done>Neon project exists, .env.local has both connection strings populated</done>
</task>

<task type="auto">
  <name>Task 2: Fix prisma/schema.prisma datasource block</name>
  <read_first>
    prisma/schema.prisma (read the current broken datasource block — lines 6-8)
    .planning/phases/01-foundation/01-RESEARCH.md (section: Pattern 1 — Schema Datasource Block)
  </read_first>
  <action>
    The current datasource block is missing both url and directUrl fields:

    CURRENT (broken):
    ```prisma
    datasource db {
      provider = "postgresql"
    }
    ```

    Replace it with (per DB-01, DB-03):
    ```prisma
    datasource db {
      provider  = "postgresql"
      url       = env("DATABASE_URL")
      directUrl = env("DIRECT_URL")
    }
    ```

    Leave everything else in the file unchanged. The 5 models (Lead, UserProfile, Reading, CreditPack, Payment) are correct and must not be touched.

  </action>
  <verify>
    <automated>grep -n "directUrl" /Users/luana/Documents/Code\ Projects/MaosFalam/prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - grep "url       = env" prisma/schema.prisma returns a line with DATABASE_URL
    - grep "directUrl = env" prisma/schema.prisma returns a line with DIRECT_URL
    - The datasource block has exactly 3 fields: provider, url, directUrl
    - All 5 model blocks (Lead, UserProfile, Reading, CreditPack, Payment) remain unchanged
    - npm run type-check passes after this change (no schema-related type errors)
  </acceptance_criteria>
  <done>prisma/schema.prisma datasource block contains url and directUrl pointing to env vars</done>
</task>

<task type="auto">
  <name>Task 3: Fix prisma.config.ts to use DIRECT_URL</name>
  <read_first>
    prisma.config.ts (read the current file — uses DATABASE_URL which is wrong)
    .planning/phases/01-foundation/01-RESEARCH.md (section: Pattern 2 — prisma.config.ts)
  </read_first>
  <action>
    The current prisma.config.ts uses DATABASE_URL (pooled) for the CLI datasource override.
    This MUST be DIRECT_URL (unpooled) — using the pooled URL for migrations causes P1001 timeout errors because PgBouncer does not support DDL transactions.

    CURRENT (wrong):
    ```typescript
    datasource: {
      url: process.env["DATABASE_URL"],
    },
    ```

    Replace with (per DB-03):
    ```typescript
    datasource: {
      url: process.env["DIRECT_URL"],
    },
    ```

    The full corrected file content:
    ```typescript
    import "dotenv/config";
    import { defineConfig } from "prisma/config";

    export default defineConfig({
      schema: "prisma/schema.prisma",
      migrations: {
        path: "prisma/migrations",
      },
      datasource: {
        url: process.env["DIRECT_URL"],
      },
    });
    ```

    Note: The comment "This file was generated by Prisma..." at the top may be removed — it was misleading.

  </action>
  <verify>
    <automated>grep "DIRECT_URL" /Users/luana/Documents/Code\ Projects/MaosFalam/prisma.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep "DIRECT_URL" prisma.config.ts returns a matching line
    - grep "DATABASE_URL" prisma.config.ts returns no match (DATABASE_URL must NOT appear in this file)
    - The file has exactly: import "dotenv/config", import { defineConfig }, and the defineConfig call with schema, migrations.path, and datasource.url = DIRECT_URL
  </acceptance_criteria>
  <done>prisma.config.ts datasource.url points to DIRECT_URL, not the pooled DATABASE_URL</done>
</task>

<task type="auto">
  <name>Task 4: Add redact config to Pino logger (SEC-07)</name>
  <read_first>
    src/server/lib/logger.ts (read current file — missing redact config)
    .planning/phases/01-foundation/01-RESEARCH.md (section: Pattern 3 — Pino Redact Config)
  </read_first>
  <action>
    The current logger.ts has no redact configuration. Any call like logger.info({ name: 'user' }) would leak PII to Vercel logs. SEC-07 requires this be fixed before any API routes are written.

    CURRENT (incomplete):
    ```typescript
    import pino from "pino";

    export const logger = pino({
      level: process.env.LOG_LEVEL || "info",
      transport:
        process.env.NODE_ENV === "development"
          ? { target: "pino-pretty", options: { colorize: true } }
          : undefined,
    });

    // REGRA: nunca logar nome, email, CPF, ou qualquer dado pessoal. Só IDs e ações.
    ```

    Replace with (per SEC-07, INFRA-01):
    ```typescript
    import pino from "pino";

    export const logger = pino({
      level: process.env.LOG_LEVEL || "info",
      redact: {
        paths: ["name", "email", "cpf", "phone", "*.name", "*.email", "*.cpf", "*.phone"],
        censor: "[REDACTED]",
      },
      transport:
        process.env.NODE_ENV === "development"
          ? { target: "pino-pretty", options: { colorize: true } }
          : undefined,
    });

    // REGRA: nunca logar nome, email, CPF, ou qualquer dado pessoal. Só IDs e ações.
    ```

    The redact paths cover: root-level fields AND one-level-deep nested fields (*.name etc).
    This ensures logger.info({ user: { name: 'Alice' } }) also redacts the name.

  </action>
  <verify>
    <automated>grep "redact" /Users/luana/Documents/Code\ Projects/MaosFalam/src/server/lib/logger.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep "redact" src/server/lib/logger.ts returns lines showing the redact object
    - The redact.paths array contains at minimum: "name", "email", "cpf", "phone", "*.name", "*.email", "*.cpf", "*.phone"
    - redact.censor is "[REDACTED]"
    - npm run type-check passes (pino types accept this config)
    - The transport config (pino-pretty in dev) is unchanged
  </acceptance_criteria>
  <done>Pino logger redacts PII fields before serialization — SEC-07 satisfied before any API routes ship</done>
</task>

<task type="auto">
  <name>Task 5: Add DIRECT_URL to .env.example</name>
  <read_first>
    .env.example (read current file — missing DIRECT_URL)
    .planning/phases/01-foundation/01-RESEARCH.md (section: Pitfall 3 — DIRECT_URL Not in .env.example)
  </read_first>
  <action>
    The current .env.example is missing DIRECT_URL. New developers will copy this file, set only DATABASE_URL, and hit silent migration failures with no explanation.

    CURRENT .env.example (Neon section):
    ```
    # Banco (Neon)
    DATABASE_URL=
    ```

    Replace the Neon section with (per DB-03, INFRA-02):
    ```
    # Banco (Neon)
    # URL pooled — usado pelo Prisma client em runtime (via PgBouncer, hostname com "-pooler")
    DATABASE_URL=postgresql://user:pass@ep-xxx.pooler.neon.tech/maosfalam?sslmode=require
    # URL direta — usada APENAS pelo Prisma CLI (migrations). Hostname SEM "-pooler"
    DIRECT_URL=postgresql://user:pass@ep-xxx.neon.tech/maosfalam?sslmode=require
    ```

    All other sections in .env.example remain unchanged.

  </action>
  <verify>
    <automated>grep "DIRECT_URL" /Users/luana/Documents/Code\ Projects/MaosFalam/.env.example</automated>
  </verify>
  <acceptance_criteria>
    - grep "DIRECT_URL" .env.example returns a line with DIRECT_URL= and an example URL
    - The comment before DIRECT_URL explains it is for Prisma CLI / migrations only (not runtime)
    - The DATABASE_URL comment explains it is pooled / used by Prisma client at runtime
    - All other vars (Clerk, OpenAI, AbacatePay, Resend, App) remain unchanged
  </acceptance_criteria>
  <done>.env.example documents both DATABASE_URL and DIRECT_URL with explanatory comments</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. grep "directUrl" prisma/schema.prisma — must return a line with DIRECT_URL
2. grep "DIRECT_URL" prisma.config.ts — must return a match; grep "DATABASE_URL" prisma.config.ts — must return NO match
3. grep "redact" src/server/lib/logger.ts — must return lines with redact config
4. grep "DIRECT_URL" .env.example — must return a line with an example URL
5. npm run type-check — must pass with 0 errors
6. npm run lint — must pass (INFRA-03 already active)
   </verification>

<success_criteria>

- prisma/schema.prisma datasource has url and directUrl
- prisma.config.ts datasource.url uses DIRECT_URL (unpooled)
- src/server/lib/logger.ts has redact config covering name/email/cpf/phone at root and \* paths
- .env.example has DIRECT_URL with explanatory comments
- .env.local has real Neon values for both DATABASE_URL and DIRECT_URL
- npm run type-check passes
- npm run lint passes
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
