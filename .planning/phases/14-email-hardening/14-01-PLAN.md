---
phase: 14-email-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/lib/resend.ts
  - src/server/lib/resend.test.ts
  - src/app/api/webhook/clerk/route.ts
  - src/app/api/webhook/clerk/__tests__/route.test.ts
  - src/app/api/reading/capture/route.ts
  - src/app/api/reading/capture/route.test.ts
  - src/app/api/webhook/abacatepay/route.ts
  - src/app/api/webhook/abacatepay/__tests__/route.test.ts
autonomous: true
requirements:
  - EMAIL-01
  - EMAIL-02
  - EMAIL-03
  - EMAIL-04

must_haves:
  truths:
    - "Payment confirmation email is sent after webhook processes checkout.completed"
    - "Welcome email is sent after first Clerk account creation"
    - "Lead reading email is only sent if lead.emailOptIn === true"
    - "Welcome email is only sent if lead.emailOptIn === true"
    - "Payment email always sends (transactional, no opt-in gate)"
    - "Resend failure does not block any main flow"
    - "If RESEND_API_KEY is missing, emails are silently skipped"
  artifacts:
    - path: "src/server/lib/resend.ts"
      provides: "Hardened email wrapper with retry, API key guard, opt-in gate, sendWelcome"
      exports: ["sendPaymentConfirmed", "sendLeadReading", "sendWelcome"]
    - path: "src/server/lib/resend.test.ts"
      provides: "Unit tests for resend wrapper"
      min_lines: 50
    - path: "src/app/api/webhook/clerk/route.ts"
      provides: "Clerk webhook handler for user.created event"
      exports: ["POST"]
    - path: "src/app/api/webhook/clerk/__tests__/route.test.ts"
      provides: "Tests for Clerk webhook"
      min_lines: 30
  key_links:
    - from: "src/app/api/webhook/abacatepay/route.ts"
      to: "src/server/lib/resend.ts"
      via: "sendPaymentConfirmed (already wired, no opt-in gate)"
      pattern: "sendPaymentConfirmed"
    - from: "src/app/api/reading/capture/route.ts"
      to: "src/server/lib/resend.ts"
      via: "sendLeadReading (add opt-in check)"
      pattern: "sendLeadReading"
    - from: "src/app/api/webhook/clerk/route.ts"
      to: "src/server/lib/resend.ts"
      via: "sendWelcome (new)"
      pattern: "sendWelcome"
---

<objective>
Harden the Resend email wrapper with retry, API key guard, and opt-in gating. Add welcome email template. Create Clerk webhook for user.created event. Wire opt-in checks to existing email call sites.

Purpose: Complete EMAIL-01 through EMAIL-04 requirements. Emails must never block main flows, respect opt-in preferences, and follow brand voice.
Output: Hardened resend.ts, Clerk webhook route, updated capture route with opt-in check, tests for all.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/brand-voice.md
@src/server/lib/resend.ts
@src/server/lib/logger.ts
@src/server/lib/auth.ts
@src/app/api/webhook/abacatepay/route.ts
@src/app/api/reading/capture/route.ts
@src/app/api/lead/register/route.ts

<interfaces>
<!-- Existing types/contracts the executor needs -->

From src/server/lib/resend.ts (current exports — will be modified):

```typescript
export async function sendPaymentConfirmed(
  to: string,
  name: string,
  readingUrl: string,
): Promise<void>;
export async function sendLeadReading(to: string, name: string, readingUrl: string): Promise<void>;
```

From src/server/lib/logger.ts:

```typescript
export const logger: {
  info(obj: Record<string, unknown>, msg: string): void;
  warn(obj: Record<string, unknown> | string, msg?: string): void;
  error(obj: Record<string, unknown>, msg: string): void;
};
```

From src/server/lib/prisma.ts:

```typescript
export const prisma: PrismaClient;
```

From Prisma Lead model (relevant fields):

```typescript
interface Lead {
  id: string;
  name: string;
  email: string;
  emailOptIn: boolean | null; // true = opted in, null/false = not
  clerkUserId: string | null;
}
```

From Prisma UserProfile model (relevant fields):

```typescript
interface UserProfile {
  clerkUserId: string;
  lead: Lead | null; // via include
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden resend.ts — API key guard, retry, opt-in gate, sendWelcome</name>
  <files>src/server/lib/resend.ts, src/server/lib/resend.test.ts</files>
  <behavior>
    - Test: sendEmail skips silently (no throw) when RESEND_API_KEY is undefined or empty
    - Test: sendEmail retries once on fetch failure (network error), then logs and returns without throwing
    - Test: sendEmail retries once on 5xx response, then logs and returns without throwing
    - Test: sendEmail does NOT retry on 4xx response (client error, no point retrying)
    - Test: sendPaymentConfirmed calls sendEmail with correct subject/html (brand voice)
    - Test: sendLeadReading calls sendEmail with correct subject/html (brand voice)
    - Test: sendWelcome calls sendEmail with correct subject/html (brand voice, includes name)
  </behavior>
  <action>
    Modify `src/server/lib/resend.ts` (per EMAIL-04):

    1. **API key guard** at top of `sendEmail()`: if `!process.env.RESEND_API_KEY`, log info "RESEND_API_KEY not set, skipping email" and return immediately. No throw.

    2. **Retry logic** in `sendEmail()`: wrap the fetch in a loop (max 2 attempts). On network error (catch) or 5xx response, wait 1 second (`await new Promise(r => setTimeout(r, 1000))`), retry once. On 4xx, do NOT retry. After final failure, log error and return (never throw). The outer try/catch already exists and swallows errors — keep that.

    3. **Add `sendWelcome(to, name)` export** (per EMAIL-02): subject "Bem-vinda, {name}. Suas maos estavam esperando." HTML template in brand voice (cigana): "Agora voce tem onde guardar o que suas maos dizem. Sua conta ta pronta. Volte quando quiser. Eu vou estar aqui." Link to /conta/leituras. Same dark styling as existing templates (background #110C1A, color #E8DFD0, link color #C9A24A).

    4. Keep existing `sendPaymentConfirmed` and `sendLeadReading` unchanged (templates are good).

    5. Do NOT add opt-in gating inside resend.ts — callers decide (resend.ts is a pure email sender). Opt-in checks happen at call sites (Task 2 and Task 3).

    Create `src/server/lib/resend.test.ts` with tests for the behaviors above. Mock `global.fetch`. Test API key guard by deleting env var.

  </action>
  <verify>
    <automated>cd /Users/luana/Documents/Code\ Projects/MaosFalam && npx vitest run src/server/lib/resend.test.ts</automated>
  </verify>
  <done>resend.ts has API key guard (skip if missing), 1x retry on transient failure, sendWelcome export. All tests pass. No existing caller behavior changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Clerk webhook for user.created — wire welcome email with opt-in check</name>
  <files>src/app/api/webhook/clerk/route.ts, src/app/api/webhook/clerk/__tests__/route.test.ts, src/proxy.ts</files>
  <behavior>
    - Test: POST with valid Clerk webhook payload (event user.created) sends welcome email via sendWelcome
    - Test: POST with user.created but no matching lead (no emailOptIn) skips email silently
    - Test: POST with user.created where lead.emailOptIn === false skips email (per EMAIL-03)
    - Test: POST with user.created where lead.emailOptIn === true sends welcome email
    - Test: POST with non-user.created event returns 200 without processing
    - Test: POST with invalid/missing svix headers returns 400
  </behavior>
  <action>
    Create `src/app/api/webhook/clerk/route.ts` (per EMAIL-02, EMAIL-03):

    1. **Clerk webhook verification**: Use Clerk's svix-based webhook verification. Import `Webhook` from `svix` (already available via @clerk/nextjs dependency — check node_modules/svix). Verify with `CLERK_WEBHOOK_SECRET` env var. If verification fails, return 400.

    IMPORTANT: If svix is NOT available as a transitive dependency, use raw HMAC verification instead. Check `node_modules/svix/package.json` first. If not found, install svix: `npm install svix`. This is the only acceptable new dependency (Clerk docs require it for webhook verification).

    2. **Event filtering**: only process `user.created` events. Others return 200 OK.

    3. **Welcome email logic**:
       - Extract `email_addresses[0].email_address` and `first_name` from Clerk payload
       - Look up lead by email: `prisma.lead.findFirst({ where: { email } })`
       - If lead exists AND `lead.emailOptIn === true`, call `sendWelcome(email, firstName)` (fire-and-forget, no await needed since sendEmail never throws)
       - If no lead or emailOptIn is false/null, skip silently (log info)

    4. **Non-blocking**: sendWelcome is fire-and-forget. Route always returns 200 after processing.

    5. **Update proxy.ts**: The webhook route `/api/webhook/clerk` must NOT be protected by Clerk auth (it comes from Clerk itself, verified by svix). Check that `isProtectedRoute` matcher does NOT match `/api/webhook/*`. Current matcher is `/api/credits/(.*)` and `/api/user/(.*)` — so `/api/webhook/clerk` is already unprotected. No change needed to proxy.ts.

    6. **Env var**: `CLERK_WEBHOOK_SECRET` — document in task that user needs to configure this in Clerk Dashboard (Settings > Webhooks > create endpoint pointing to `{BASE_URL}/api/webhook/clerk`, subscribe to `user.created`, copy signing secret to env var).

    Create `src/app/api/webhook/clerk/__tests__/route.test.ts` with tests. Mock svix Webhook.verify, mock prisma, mock sendWelcome.

  </action>
  <verify>
    <automated>cd /Users/luana/Documents/Code\ Projects/MaosFalam && npx vitest run src/app/api/webhook/clerk/__tests__/route.test.ts</automated>
  </verify>
  <done>Clerk webhook handles user.created, sends welcome email only if lead.emailOptIn === true. Invalid signatures rejected. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Wire opt-in check to lead reading email in capture route + verify payment email</name>
  <files>src/app/api/reading/capture/route.ts, src/app/api/reading/capture/route.test.ts, src/app/api/webhook/abacatepay/__tests__/route.test.ts</files>
  <action>
    Per EMAIL-03: "Emails so enviados se lead.email_opt_in === true (exceto transacionais de pagamento)."

    1. **Capture route** (`src/app/api/reading/capture/route.ts`): The lead reading email (sendLeadReading) on line ~102-109 currently sends to ANY lead with an email. Add opt-in check:
       ```
       if (lead?.email && lead?.emailOptIn === true) {
         sendLeadReading(lead.email, lead.name, `${baseUrl}/ler/resultado/${reading.id}`);
       }
       ```
       Change from `if (lead?.email)` to `if (lead?.email && lead.emailOptIn === true)`.

    2. **Payment webhook** (`src/app/api/webhook/abacatepay/route.ts`): The sendPaymentConfirmed call on line ~165 does NOT need opt-in check — it is a transactional email (purchase confirmation). Leave it as-is. This is correct per EMAIL-03 "exceto transacionais de pagamento".

    3. **Update capture route test** (`route.test.ts`): Add test case verifying sendLeadReading is NOT called when lead.emailOptIn is false. Add test case verifying it IS called when emailOptIn is true.

    4. **Update webhook test**: Add a comment or assertion confirming sendPaymentConfirmed is called regardless of opt-in (documenting the EMAIL-03 exception).

    5. **Build check**: Run `npm run build` to verify no type errors.

  </action>
  <verify>
    <automated>cd /Users/luana/Documents/Code\ Projects/MaosFalam && npx vitest run src/app/api/reading/capture/route.test.ts src/app/api/webhook/abacatepay/__tests__/route.test.ts && npm run build</automated>
  </verify>
  <done>Lead reading email respects emailOptIn. Payment email sends unconditionally (transactional). All tests pass. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/server/lib/resend.test.ts` — all resend wrapper tests pass
2. `npx vitest run src/app/api/webhook/clerk/__tests__/route.test.ts` — Clerk webhook tests pass
3. `npx vitest run src/app/api/reading/capture/route.test.ts` — capture route tests pass (including opt-in check)
4. `npx vitest run src/app/api/webhook/abacatepay/__tests__/route.test.ts` — webhook tests pass
5. `npm run build` — clean build, no type errors
6. `npm run lint` — no lint violations
</verification>

<success_criteria>

- resend.ts: API key guard (skip silently), 1x retry on transient failure, sendWelcome export
- Clerk webhook at /api/webhook/clerk handles user.created → sendWelcome (with opt-in check)
- Capture route: sendLeadReading only if emailOptIn === true
- Payment webhook: sendPaymentConfirmed always (no opt-in gate, transactional)
- All email failures are caught and logged, never throw or block main flow
- All tests pass, build succeeds, lint clean
  </success_criteria>

<output>
After completion, create `.planning/phases/14-email-hardening/14-01-SUMMARY.md`
</output>
