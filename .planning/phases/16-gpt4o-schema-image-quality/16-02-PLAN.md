---
phase: 16-gpt4o-schema-image-quality
plan: "02"
type: execute
wave: 2
depends_on:
  - "16-01"
files_modified:
  - src/lib/mediapipe.ts
  - src/lib/normalize-image.ts
  - src/lib/photo-store.ts
  - src/lib/reading-client.ts
  - src/app/api/reading/capture/route.ts
  - src/app/ler/scan/page.tsx
autonomous: true
requirements:
  - IMG-01
  - IMG-02
  - IMG-03

must_haves:
  truths:
    - "captureFrame gera JPEG com quality 0.92"
    - "normalizeImage redimensiona para max 2048px (nao 1280px)"
    - "A rota /api/reading/capture rejeita payloads acima de 4MB com status 413"
    - "element_hint desapareceu de todos os arquivos (route, client, scan, photo-store)"
    - "npm run type-check e npm run build passam sem erros"
  artifacts:
    - path: src/lib/mediapipe.ts
      provides: captureFrame com quality 0.92; computeElementHint e dist3D ainda presentes (removidos na Phase 17)
    - path: src/lib/normalize-image.ts
      provides: normalizeImage com maxWidth 2048, quality 0.92
    - path: src/lib/photo-store.ts
      provides: photo-store sem _elementHint, sem setElementHint, sem getElementHint
    - path: src/lib/reading-client.ts
      provides: captureReading sem element_hint no payload
    - path: src/app/api/reading/capture/route.ts
      provides: body limit 4MB; sem element_hint no Zod schema; analyzeHand sem terceiro arg; sem finalAttributes override
    - path: src/app/ler/scan/page.tsx
      provides: scan page sem getElementHint, sem element_hint no captureReading call
  key_links:
    - from: src/app/ler/scan/page.tsx
      to: src/lib/photo-store.ts
      via: "getPhoto() only — getElementHint removed"
      pattern: "getPhoto\\(\\)"
    - from: src/app/api/reading/capture/route.ts
      to: src/server/lib/openai.ts
      via: "analyzeHand(data.photo_base64, data.dominant_hand) — sem terceiro arg"
      pattern: "analyzeHand\\(data\\.photo_base64"
---

<objective>
Subir qualidade de imagem (JPEG 0.92, max 2048px, body limit 4MB) e limpar todos os rastros de element_hint do pipeline (photo-store, reading-client, scan page, capture route).

Purpose: Fotos de maior qualidade melhoram a precisao do GPT-4o. Remover element_hint completa a separacao entre MediaPipe (validacao) e GPT-4o (classificacao) iniciada no plan 01.
Output: Pipeline sem element_hint de ponta a ponta. Imagens chegam ao GPT-4o com qualidade 0.92 e max 2048px.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/16-gpt4o-schema-image-quality/16-CONTEXT.md
@.planning/phases/16-gpt4o-schema-image-quality/16-01-SUMMARY.md

<interfaces>
<!-- Estado atual dos arquivos antes da modificacao -->

From src/lib/mediapipe.ts (funcao captureFrame atual):

```typescript
export function captureFrame(
  video: HTMLVideoElement,
  canvas: HTMLCanvasElement,
  mirrored: boolean,
  quality = 0.82, // <- MUDAR para 0.92
): string;
```

Nota: computeElementHint e dist3D FICAM em mediapipe.ts nesta phase — sao removidos na Phase 17.
HandElement type exportado de mediapipe.ts tambem fica — Phase 17 remove.

From src/lib/normalize-image.ts (valores atuais):

```typescript
const maxWidth = 1280; // <- MUDAR para 2048
// ...
const dataUrl = canvas.toDataURL("image/jpeg", 0.85); // <- MUDAR para 0.92
```

From src/lib/photo-store.ts (estado atual — remover elementHint):

```typescript
let _photo: string | null = null;
let _elementHint: HandElement | null = null;  // REMOVER

export function setElementHint(hint: HandElement): void { ... }  // REMOVER
export function getElementHint(): HandElement | undefined { ... }  // REMOVER
export function clearPhotoStore(): void {
  _photo = null;
  _elementHint = null;  // <- limpar esta linha tambem
}
```

From src/lib/reading-client.ts (campo a remover):

```typescript
export async function captureReading(data: {
  // ...
  element_hint?: "fire" | "water" | "earth" | "air"; // REMOVER
});
```

From src/app/api/reading/capture/route.ts (mudancas):

```typescript
// Zod schema — REMOVER:
element_hint: z.enum(["fire", "water", "earth", "air"]).optional(),

// Body limit — MUDAR de 2MB para 4MB:
if (bodyStr.length > 2 * 1024 * 1024) {  // 2 -> 4

// analyzeHand call — REMOVER terceiro arg:
const attributes = await analyzeHand(data.photo_base64, data.dominant_hand, data.element_hint);
// -> const attributes = await analyzeHand(data.photo_base64, data.dominant_hand);

// Bloco finalAttributes — REMOVER inteiro:
const finalAttributes = data.element_hint
  ? { ...attributes, element: data.element_hint }
  : attributes;
if (data.element_hint) {
  logger.info({ gptElement: attributes.element, mediapiElement: data.element_hint }, "...");
}

// Substituir todas as referencias a finalAttributes por attributes nas linhas seguintes
```

From src/app/ler/scan/page.tsx (mudancas):

```typescript
// REMOVER import:
import { clearPhotoStore, getElementHint, getPhoto } from "@/lib/photo-store";
// -> import { clearPhotoStore, getPhoto } from "@/lib/photo-store";

// REMOVER:
const elementHint = getElementHint();

// REMOVER element_hint do captureReading call:
captureReading({
  // ...
  element_hint: elementHint, // REMOVER esta linha
});
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Image quality — captureFrame 0.92 + normalizeImage 2048px + limpar photo-store</name>
  <files>src/lib/mediapipe.ts, src/lib/normalize-image.ts, src/lib/photo-store.ts</files>
  <action>
### mediapipe.ts

Mudar o default de quality no captureFrame de 0.82 para 0.92:

```typescript
export function captureFrame(
  video: HTMLVideoElement,
  canvas: HTMLCanvasElement,
  mirrored: boolean,
  quality = 0.92, // era 0.82
): string;
```

NAO remover computeElementHint, dist3D, HandElement type — isso e trabalho da Phase 17.

### normalize-image.ts

Duas mudancas:

1. `const maxWidth = 1280` → `const maxWidth = 2048`
2. `canvas.toDataURL("image/jpeg", 0.85)` → `canvas.toDataURL("image/jpeg", 0.92)`

Atualizar comentario no topo do arquivo: "Resizes to max 2048px wide at JPEG 0.92 quality" (era "max 1280px" e "JPEG 0.85").

### photo-store.ts

Remover completamente o suporte a element hint:

- Remover a linha `type HandElement = "fire" | "water" | "earth" | "air";` (local)
- Remover `let _elementHint: HandElement | null = null;`
- Remover funcao `setElementHint`
- Remover funcao `getElementHint`
- Simplificar `clearPhotoStore` para apenas `_photo = null;`

O arquivo simplificado deve exportar somente: `setPhoto`, `getPhoto`, `clearPhotoStore`.

Comentario no topo pode ser simplificado — remover mencao a element hint se existir.
</action>
<verify>
<automated>cd "/Users/luana/Documents/Code Projects/Paritech projs/MaosFalam" && npm run type-check 2>&1 | grep "photo-store\|mediapipe\|normalize" | head -10</automated>
</verify>
<done>

- captureFrame default quality = 0.92
- normalizeImage usa maxWidth=2048 e quality=0.92
- photo-store exporta somente setPhoto, getPhoto, clearPhotoStore
- type-check sem erros nos 3 arquivos modificados
  </done>
  </task>

<task type="auto">
  <name>Task 2: Cleanup de element_hint no capture route, reading-client e scan page</name>
  <files>src/app/api/reading/capture/route.ts, src/lib/reading-client.ts, src/app/ler/scan/page.tsx</files>
  <action>
### capture/route.ts

1. Body limit: alterar `2 * 1024 * 1024` para `4 * 1024 * 1024`
   Atualizar comentario: "Limits request body to 4MB — JPEG 0.92 at 2048px is ~1-2MB, 4MB gives headroom"

2. Zod schema: remover `element_hint: z.enum(["fire", "water", "earth", "air"]).optional()`

3. analyzeHand call: remover terceiro argumento:

   ```typescript
   const attributes = await analyzeHand(data.photo_base64, data.dominant_hand);
   ```

4. Remover o bloco `finalAttributes` inteiro (as ~8 linhas com const finalAttributes + if element_hint + logger.info).

5. Substituir todas as referencias a `finalAttributes` por `attributes` nas linhas seguintes:
   - `if (finalAttributes.confidence < 0.3)` → `if (attributes.confidence < 0.3)`
   - `const report = selectBlocks(finalAttributes, ...)` → `const report = selectBlocks(attributes, ...)`
   - `attributes: JSON.parse(JSON.stringify(finalAttributes))` → `attributes: JSON.parse(JSON.stringify(attributes))`
   - `confidence: finalAttributes.confidence` → `confidence: attributes.confidence`
   - No logger.info: `element: finalAttributes.element` → `element: attributes.element`, `confidence: finalAttributes.confidence` → `confidence: attributes.confidence`

Per D-05 em 16-CONTEXT.md: adicionar `attributes` ao response JSON para debug/teste:

```typescript
return NextResponse.json({
  reading_id: reading.id,
  report,
  tier,
  attributes, // incluir para debug — GPT-4o attributes com primary_type, element, etc.
});
```

### reading-client.ts

Remover `element_hint?: "fire" | "water" | "earth" | "air";` do tipo de parametro de captureReading.
Remover `element_hint: elementHint,` do objeto passado ao JSON.stringify (se presente).

Verificar que o tipo de retorno ainda inclui `{ reading_id: string; report: ReportJSON; tier?: string }` — nao precisa adicionar attributes ao tipo de retorno do client (e opcional para debug no server).

### scan/page.tsx

1. Remover `getElementHint` do import de photo-store:

   ```typescript
   import { clearPhotoStore, getPhoto } from "@/lib/photo-store";
   ```

2. Remover a linha `const elementHint = getElementHint();` no Effect 1.

3. Remover `element_hint: elementHint,` do objeto passado a captureReading.

Verificar que `clearPhotoStore()` ainda e chamado imediatamente apos `getPhoto()` — manter esse comportamento.
</action>
<verify>
<automated>cd "/Users/luana/Documents/Code Projects/Paritech projs/MaosFalam" && npm run type-check 2>&1 | tail -5 && npm run build 2>&1 | tail -10</automated>
</verify>
<done>

- capture route tem body limit 4MB, sem element_hint no Zod, analyzeHand sem terceiro arg, sem bloco finalAttributes
- reading-client.ts sem element_hint no tipo e no payload
- scan/page.tsx sem getElementHint, sem element_hint no captureReading call
- npm run type-check passa sem erros
- npm run build passa sem erros
  </done>
  </task>

</tasks>

<verification>
Verificar que element_hint foi removido de todos os lugares:
```bash
grep -rn "element_hint\|elementHint\|getElementHint\|setElementHint" \
  "/Users/luana/Documents/Code Projects/Paritech projs/MaosFalam/src" \
  --include="*.ts" --include="*.tsx"
```
Resultado esperado: zero linhas (ou apenas comentarios/historico — nenhuma linha de codigo ativa).

Verificar quality e maxWidth:

```bash
grep -n "0\.92\|2048" \
  "/Users/luana/Documents/Code Projects/Paritech projs/MaosFalam/src/lib/mediapipe.ts" \
  "/Users/luana/Documents/Code Projects/Paritech projs/MaosFalam/src/lib/normalize-image.ts"
```

Verificar body limit:

```bash
grep -n "1024 \* 1024" "/Users/luana/Documents/Code Projects/Paritech projs/MaosFalam/src/app/api/reading/capture/route.ts"
```

Deve mostrar `4 * 1024 * 1024`.

Build final:

```bash
cd "/Users/luana/Documents/Code Projects/Paritech projs/MaosFalam" && npm run build
```

</verification>

<success_criteria>

1. captureFrame quality default = 0.92 (era 0.82)
2. normalizeImage: maxWidth=2048 (era 1280), quality=0.92 (era 0.85)
3. capture route body limit = 4MB (era 2MB)
4. grep por "element_hint" em src/ retorna zero resultados de codigo ativo
5. analyzeHand chamado sem terceiro argumento em capture/route.ts
6. npm run type-check e npm run build passam sem erros
   </success_criteria>

<output>
Apos completar, criar `.planning/phases/16-gpt4o-schema-image-quality/16-02-SUMMARY.md`
</output>
