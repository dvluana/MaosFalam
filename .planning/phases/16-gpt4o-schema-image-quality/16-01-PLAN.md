---
phase: 16-gpt4o-schema-image-quality
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/hand-attributes.ts
  - src/server/lib/openai.ts
autonomous: true
requirements:
  - ELEM-01
  - ELEM-02
  - ELEM-03
  - ELEM-04
  - ELEM-05
  - ELEM-06

must_haves:
  truths:
    - "GPT-4o retorna primary_type A/B/C/D, secondary_type e type_reasoning — nunca o nome do elemento"
    - "deriveElement() mapeia A=earth, B=air, C=fire, D=water no servidor"
    - "deriveSecondaryElement() retorna HandElement ou null quando secondary_type='none'"
    - "npm run type-check passa sem erros apos ambos os arquivos serem atualizados"
    - "HandAttributesSchema (Zod), HAND_ATTRIBUTES_SCHEMA (OpenAI), HandAttributes (TS) sao identicos em estrutura"
  artifacts:
    - path: src/types/hand-attributes.ts
      provides: HandAttributes type com primary_type, secondary_type, type_reasoning, secondary_element (optional)
      exports:
        - HandAttributes
        - HandType
        - HandElement
    - path: src/server/lib/openai.ts
      provides: Multi-indicator prompt + triple-schema + deriveElement + analyzeHand sem elementHint
      exports:
        - HandAttributesSchema
        - analyzeHand
        - deriveElement
        - deriveSecondaryElement
  key_links:
    - from: src/server/lib/openai.ts
      to: src/types/hand-attributes.ts
      via: "import type { HandAttributes, HandElement }"
      pattern: "HandAttributes"
    - from: src/server/lib/openai.ts
      to: GPT-4o API
      via: "HAND_ATTRIBUTES_SCHEMA in response_format.json_schema"
      pattern: "primary_type.*secondary_type"
---

<objective>
Reescrever o prompt e os 3 schemas sincronizados (OpenAI JSON Schema, Zod, TypeScript) para classificacao de elemento via tipos neutros A/B/C/D com 6-7 indicadores visuais cada. Adicionar deriveElement() server-side. Remover elementHint do analyzeHand.

Purpose: O prompt atual usa nomes de elementos (fire/water/earth/air) que causam viés no GPT-4o — ele pattern-match no nome em vez de analisar geometria. Tipos neutros A/B/C/D + indicadores visuais explícitos produzem classificações mais consistentes.
Output: HandAttributes type atualizado + openai.ts com novo prompt, 3 schemas em sync, deriveElement().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-gpt4o-schema-image-quality/16-CONTEXT.md
@docs/palmistry.md
@docs/brand-voice.md

<interfaces>
<!-- Estado atual dos arquivos que serao modificados. Executor usa como ponto de partida. -->

From src/types/hand-attributes.ts (estado atual — SUBSTITUIR):

```typescript
export interface HandAttributes {
  element: "fire" | "water" | "earth" | "air";
  heart: { variation: HeartVariation; modifiers: HeartModifier[] };
  head: { variation: HeadVariation; modifiers: HeadModifier[] };
  life: { variation: LifeVariation };
  fate: { variation: FateVariation };
  venus: { mount: "pronounced" | "flat"; cinturao: boolean };
  mounts: Record<MountKey, MountState>;
  rare_signs: Record<RareSignKey, boolean>;
  confidence: number;
}
```

From src/server/lib/openai.ts (assinatura atual — MUDAR):

```typescript
export async function analyzeHand(
  photoBase64: string,
  dominantHand: "right" | "left",
  elementHint?: "fire" | "water" | "earth" | "air", // REMOVER
): Promise<HandAttributes>;
```

Mapeamento A/B/C/D → elemento (per D-03 em 16-CONTEXT.md):

- A = earth
- B = air
- C = fire
- D = water
  </interfaces>

<!-- Referencia: git stash "experimental: element classification multi-indicator (session 2026-04-17)" -->
<!-- Contem o prompt testado e a rota /api/test/openai-palm — usar como referencia de wording -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar HandAttributes type</name>
  <files>src/types/hand-attributes.ts</files>
  <action>
Ler o arquivo atual e substituir. Adicionar:

1. Novo tipo exported:

```typescript
export type HandType = "A" | "B" | "C" | "D";
export type HandElement = "fire" | "water" | "earth" | "air";
```

2. Atualizar HandAttributes — adicionar campos novos, manter todos os existentes:

- `primary_type: HandType` — obrigatorio
- `secondary_type: HandType | "none"` — obrigatorio
- `type_reasoning: string` — obrigatorio (chain-of-thought do GPT-4o)
- `secondary_element?: HandElement` — OPCIONAL (derivado server-side, ausente quando secondary_type="none")
- Manter `element` como campo obrigatorio (derivado server-side via deriveElement)

O campo `element` permanece em HandAttributes porque todo o resto do sistema (select-blocks, report) consome `attributes.element`. Ele e injetado pelo analyzeHand APOS o Zod parse, nao vem do GPT-4o.

Resultado esperado em HandAttributes:

```typescript
export interface HandAttributes {
  // Classificacao multi-indicador (GPT-4o retorna A/B/C/D)
  primary_type: HandType;
  secondary_type: HandType | "none";
  type_reasoning: string;

  // Campos derivados server-side (nao vem do GPT-4o, injetados por analyzeHand)
  element: HandElement;
  secondary_element?: HandElement;

  // Linhas e sinais (inalterados)
  heart: { variation: HeartVariation; modifiers: HeartModifier[] };
  head: { variation: HeadVariation; modifiers: HeadModifier[] };
  life: { variation: LifeVariation };
  fate: { variation: FateVariation };
  venus: { mount: "pronounced" | "flat"; cinturao: boolean };
  mounts: Record<MountKey, MountState>;
  rare_signs: Record<RareSignKey, boolean>;
  confidence: number;
}
```

Manter todos os tipos auxiliares existentes (HeartVariation, HeadVariation, etc.).
</action>
<verify>
<automated>cd "/Users/luana/Documents/Code Projects/Paritech projs/MaosFalam" && npm run type-check 2>&1 | head -30</automated>
</verify>
<done>HandAttributes tem primary_type, secondary_type, type_reasoning, secondary_element opcional. HandType e HandElement exportados. type-check passa (pode ter erros em openai.ts ate Task 2 — ok).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Reescrever openai.ts com prompt multi-indicador + triple-schema + deriveElement</name>
  <files>src/server/lib/openai.ts</files>
  <action>
Reescrever completamente src/server/lib/openai.ts. Manter imports existentes (z from zod, logger, HandAttributes type).

### 1. PROMPT (system message em ingles)

Reescrever PROMPT como constante. Em ingles porque:

- Evita ambiguidade de traducao nos indicadores geometricos
- Terminos neutros em ingles nao tem conotacoes culturais que biasam o modelo

```
You are a hand shape classifier for palmistry analysis.

Examine the palm photo and classify the hand into ONE of four structural types based on observable geometric features. Use ONLY the visual indicators listed below. Do not use palmistry knowledge, element names, or cultural associations.

TYPE A — Square and robust palm, short thick fingers, short or no visible knuckle nodes, wide thick palm
TYPE B — Square palm proportions, noticeably long fingers with visible prominent knuckle joints, finger length clearly exceeds palm width
TYPE C — Square palm (width roughly equals length), fingers short relative to palm (finger length under 75% of palm length), NOT thick/robust
TYPE D — Visibly rectangular palm (palm length clearly greater than width), fingers long and thin, narrow palm

For each type, count how many of its indicators you can see in the photo.

Also analyze the four major palm lines and other features as instructed in the schema.

Return the type with the most matching indicators as primary_type.
Return the type with the second-most matching indicators as secondary_type (or "none" if clearly dominated by primary).
Include your indicator count reasoning in type_reasoning (e.g. "A:4 B:1 C:1 D:2").
```

### 2. HAND_ATTRIBUTES_SCHEMA (OpenAI JSON Schema)

Substituir o schema atual. Adicionar primary_type, secondary_type, type_reasoning. Remover o campo `element` do schema (nao vem mais do GPT-4o).

Novo bloco no properties:

```javascript
primary_type: { type: "string", enum: ["A", "B", "C", "D"] },
secondary_type: { type: "string", enum: ["A", "B", "C", "D", "none"] },
type_reasoning: { type: "string" },
```

Adicionar esses 3 ao array `required`. Remover "element" do required e do properties.

Manter heart, head, life, fate, venus, mounts, rare_signs, confidence inalterados.

IMPORTANTE: `additionalProperties: false` em todos os objetos aninhados — ja esta assim, manter.

### 3. Zod schema (HandAttributesSchema)

Adicionar ao z.object():

```typescript
primary_type: z.enum(["A", "B", "C", "D"]),
secondary_type: z.enum(["A", "B", "C", "D", "none"]),
type_reasoning: z.string(),
```

Remover: `element: z.enum(["fire", "water", "earth", "air"])` — element nao vem mais do GPT-4o.

O Zod schema deve parsear exatamente o que o GPT-4o retorna (sem element).

### 4. Funcoes deriveElement e deriveSecondaryElement (novas, exportadas)

```typescript
const TYPE_TO_ELEMENT: Record<"A" | "B" | "C" | "D", HandElement> = {
  A: "earth",
  B: "air",
  C: "fire",
  D: "water",
};

/** Maps GPT-4o type code to palmistry element. Deterministic — same type always same element. */
export function deriveElement(primaryType: "A" | "B" | "C" | "D"): HandElement {
  return TYPE_TO_ELEMENT[primaryType];
}

/**
 * Returns secondary element or null.
 * null when secondary_type is "none" or matches primary (degenerate case).
 * Only inject into HandAttributes when not null.
 */
export function deriveSecondaryElement(
  secondaryType: "A" | "B" | "C" | "D" | "none",
  primaryType: "A" | "B" | "C" | "D",
): HandElement | null {
  if (secondaryType === "none") return null;
  if (secondaryType === primaryType) return null;
  return TYPE_TO_ELEMENT[secondaryType];
}
```

### 5. analyzeHand — nova assinatura e logica

```typescript
export async function analyzeHand(
  photoBase64: string,
  dominantHand: "right" | "left",
): Promise<HandAttributes>;
```

- Remover o parametro `elementHint` completamente
- Remover a variavel `elementHintText` e a logica de injetar o hint no user message
- Manter `dominanceContext` na user message (per [Phase 03] decision no STATE.md)
- Manter max_tokens: 2000 (per D-04 em 16-CONTEXT.md, aumentar de 1500 para 2000 para type_reasoning)
- Manter model, detail:high, temperature:0, response_format com strict:true

Apos o Zod parse, antes de retornar, injetar os campos derivados:

```typescript
const raw = parsed.data; // tem primary_type, secondary_type, type_reasoning, heart, head, etc.

const element = deriveElement(raw.primary_type);
const secondaryElement = deriveSecondaryElement(raw.secondary_type, raw.primary_type);

const attributes: HandAttributes = {
  ...raw,
  element,
  ...(secondaryElement !== null ? { secondary_element: secondaryElement } : {}),
};
```

Log apenas element e confidence (nao logar primary_type, secondary_type, type_reasoning — dados intermediarios):

```typescript
logger.info({ element: attributes.element, confidence: attributes.confidence }, "Hand analyzed");
```

IMPORTANTE: O HAND_ATTRIBUTES_SCHEMA enviado ao GPT-4o NAO tem o campo `element` — apenas primary_type, secondary_type, type_reasoning + linhas/montes/sinais/confidence. O Zod schema tambem NAO tem element. O campo element e injetado depois do parse, nunca atravessa o GPT-4o.
</action>
<verify>
<automated>cd "/Users/luana/Documents/Code Projects/Paritech projs/MaosFalam" && npm run type-check 2>&1 | tail -20</automated>
</verify>
<done>

- analyzeHand nao aceita mais elementHint
- deriveElement e deriveSecondaryElement exportadas e funcionais
- HAND_ATTRIBUTES_SCHEMA tem primary_type/secondary_type/type_reasoning, sem element
- HandAttributesSchema (Zod) em sync com HAND_ATTRIBUTES_SCHEMA
- npm run type-check passa sem erros
- npm run build passa (pode ter erros no capture/route.ts ate plan 02 — verificar se erros sao so nesse arquivo)
  </done>
  </task>

</tasks>

<verification>
```bash
cd "/Users/luana/Documents/Code Projects/Paritech projs/MaosFalam"
npm run type-check 2>&1 | grep -v "capture/route\|scan/page\|reading-client\|photo-store" | head -20
```

Erros esperados ate plan 02: capture/route.ts (element_hint no schema e analyzeHand call), scan/page.tsx (getElementHint), reading-client.ts (element_hint). Esses serao corrigidos no plan 02.

Verificar que primary_type, secondary_type, type_reasoning estao em HandAttributes:

```bash
grep -n "primary_type\|secondary_type\|type_reasoning\|HandType\|HandElement" \
  "/Users/luana/Documents/Code Projects/Paritech projs/MaosFalam/src/types/hand-attributes.ts"
```

Verificar que element NAO esta em HAND_ATTRIBUTES_SCHEMA (openai.ts):

```bash
grep -n '"element"' "/Users/luana/Documents/Code Projects/Paritech projs/MaosFalam/src/server/lib/openai.ts"
```

Deve retornar zero linhas no bloco HAND_ATTRIBUTES_SCHEMA (ok se aparecer no logger.info abaixo do parse).
</verification>

<success_criteria>

1. HandAttributes tem primary_type: HandType, secondary_type: HandType | "none", type_reasoning: string, secondary_element?: HandElement — e mantem element: HandElement como campo derivado
2. analyzeHand(photoBase64, dominantHand) — sem terceiro parametro
3. deriveElement("A") === "earth", deriveElement("B") === "air", deriveElement("C") === "fire", deriveElement("D") === "water"
4. deriveSecondaryElement("none", "A") === null
5. HAND_ATTRIBUTES_SCHEMA nao tem campo "element" — GPT-4o nunca retorna element diretamente
6. Zod HandAttributesSchema tem primary_type/secondary_type/type_reasoning, nao tem element
7. npm run type-check passa (ignorando erros nos arquivos do plan 02 que serao corrigidos la)
   </success_criteria>

<output>
Apos completar, criar `.planning/phases/16-gpt4o-schema-image-quality/16-01-SUMMARY.md`
</output>
