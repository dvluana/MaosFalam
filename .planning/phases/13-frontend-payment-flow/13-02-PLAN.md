---
phase: 13-frontend-payment-flow
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/components/reading/UpsellSection.tsx
  - src/app/ler/resultado/[id]/page.tsx
  - src/app/conta/leituras/page.tsx
autonomous: false
requirements: [FRONT-04, FRONT-05]

must_haves:
  truths:
    - "UpsellSection receives readingId as prop, not window.location regex"
    - "UpsellSection with credits calls upgradeReading and redirects to /completo"
    - "UpsellSection without credits redirects to /creditos?reading={id}"
    - "Returning to /ler/resultado/[id]?paid=1 refetches reading and redirects to /completo if premium"
    - "Returning to /conta/leituras?purchased=1 shows success toast"
  artifacts:
    - path: "src/components/reading/UpsellSection.tsx"
      provides: "Updated upsell component with readingId prop and /creditos redirect with reading param"
    - path: "src/app/ler/resultado/[id]/page.tsx"
      provides: "Payment return handling with ?paid=1 detection"
    - path: "src/app/conta/leituras/page.tsx"
      provides: "Payment return handling with ?purchased=1 toast"
  key_links:
    - from: "src/components/reading/UpsellSection.tsx"
      to: "/creditos?reading={id}"
      via: "router.push on 402 error"
      pattern: "router\\.push.*creditos.*reading"
    - from: "src/app/ler/resultado/[id]/page.tsx"
      to: "src/lib/reading-client.ts"
      via: "getReading() refetch on ?paid=1"
      pattern: "paid.*getReading"
    - from: "src/app/conta/leituras/page.tsx"
      to: "@/components/ui/ToastProvider"
      via: "showToast on ?purchased=1"
      pattern: "purchased.*showToast"
---

<objective>
Wire UpsellSection to redirect to /creditos with reading context when user has no credits. Handle return from AbacatePay: /ler/resultado/[id]?paid=1 refetches and redirects to /completo, /conta/leituras?purchased=1 shows success toast.

Purpose: Completes the payment loop — user clicks upgrade, pays, returns to their unlocked reading.
Output: UpsellSection works end-to-end. Return URLs handle post-payment state correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-frontend-payment-flow/13-RESEARCH.md
@.planning/phases/13-frontend-payment-flow/13-01-SUMMARY.md

<interfaces>
<!-- From reading-client.ts -->
```typescript
export async function upgradeReading(readingId: string): Promise<{
  tier: string;
  credits_remaining: number;
  already_premium?: boolean;
}>;

export async function getReading(id: string): Promise<Reading | null>;

````

<!-- Reading type -->
```typescript
interface Reading {
  id: string;
  tier: "free" | "premium";
  target_name?: string;
  report: ReportJSON;
  created_at: string;
}
````

<!-- Backend sets these completionUrls: -->
<!-- With readingId: /ler/resultado/${readingId}?paid=1 -->
<!-- Without readingId: /conta/leituras?purchased=1 -->

<!-- UpsellSection currently used in: -->
<!-- src/app/ler/resultado/[id]/page.tsx (line 146): <UpsellSection /> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update UpsellSection with readingId prop + /creditos redirect with reading param</name>
  <files>src/components/reading/UpsellSection.tsx, src/app/ler/resultado/[id]/page.tsx</files>
  <action>
    Update UpsellSection (per FRONT-04, research pattern):

    1. Add readingId prop to UpsellSection:
       ```
       interface UpsellSectionProps { readingId: string; }
       export default function UpsellSection({ readingId }: UpsellSectionProps)
       ```

    2. Remove the window.location.pathname regex extraction (lines 21-23 of current file). Use the prop directly.

    3. Update the 402/no-credits redirect to include reading context:
       - Current: `router.push("/creditos")`
       - New: `router.push(\`/creditos?reading=\${readingId}\`)`
       This ensures the /creditos page (rewritten in 13-01) passes readingId to initiatePurchase(), which makes the backend set completionUrl to /ler/resultado/[id]?paid=1.

    4. Keep the existing flow for users WITH credits: upgradeReading(readingId) -> router.push to /completo. This works already.

    5. Update the parent page src/app/ler/resultado/[id]/page.tsx to pass the readingId prop:
       - Current (line 146): `<UpsellSection />`
       - New: `<UpsellSection readingId={id} />`
       The `id` variable is already available in ResultadoInner (line 80 params destructure).

    6. Also check /conta/leituras/[id]/page.tsx — if it renders UpsellSection, update there too. (Based on code read: it does NOT render UpsellSection, it just redirects to /ler/resultado/[id]. No change needed.)

  </action>
  <verify>
    <automated>cd /Users/luana/Documents/Code\ Projects/MaosFalam && npm run type-check</automated>
  </verify>
  <done>
    - UpsellSection accepts readingId as prop (no window.location parsing)
    - 402 redirects to /creditos?reading={readingId}
    - Parent page passes readingId prop
    - type-check passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Handle payment return URLs (?paid=1 and ?purchased=1)</name>
  <files>src/app/ler/resultado/[id]/page.tsx, src/app/conta/leituras/page.tsx</files>
  <action>
    Handle post-payment return flows (per FRONT-05, research pitfall 1):

    **1. /ler/resultado/[id]?paid=1 — refetch + redirect to /completo**

    In src/app/ler/resultado/[id]/page.tsx, update ResultadoInner:

    - Add `useSearchParams` import (not imported yet. Add `useRouter` and `useSearchParams` from next/navigation)
    - Read `const searchParams = useSearchParams()` and `const isPaidReturn = searchParams.get("paid") === "1"`
    - Add `const router = useRouter()`
    - Add state: `paymentPending` (boolean, starts true when isPaidReturn)
    - After the initial getReading() resolves:
      - If isPaidReturn AND reading.tier === "premium": immediately router.replace(`/ler/resultado/${id}/completo`)
      - If isPaidReturn AND reading.tier === "free": webhook hasn't processed yet. Show a brief "Confirmando seu pagamento..." message. Set a 3-second timeout, then refetch getReading(id). If now premium: redirect. If still free: show message "Seu pagamento esta sendo confirmado. Recarregue em alguns instantes." with a retry button.
    - Show the "Confirmando..." state INSTEAD of the normal resultado content when paymentPending is true. Use a simple centered layout: cigana voice "Confirmando seu pagamento..." in Cormorant italic + a subtle spinner (reuse the loading pattern from the page).
    - If after retry it's still free, show the normal resultado page (free) with a toast: "Pagamento sendo processado. Atualize a pagina em alguns instantes."

    **2. /conta/leituras?purchased=1 — success toast**

    In src/app/conta/leituras/page.tsx, update LeiturasContent:

    - Add `useSearchParams` import
    - Read `const searchParams = useSearchParams()` (already using Suspense boundary, safe)
    - In useEffect (or a separate one): if searchParams.get("purchased") === "1", call showToast with variant "gold", message "Seus creditos estao esperando. Usa com sabedoria." (cigana voice). Then router.replace("/conta/leituras") to clean the URL (prevent toast on refresh).
    - showToast is already imported and available in LeiturasContent.

    **Brand voice for payment confirmation states:**
    - Confirming: "Confirmando seu pagamento..."
    - Still processing: "Seu pagamento esta sendo confirmado. Recarregue em alguns instantes."
    - Credits purchased toast: "Seus creditos estao esperando. Usa com sabedoria."

  </action>
  <verify>
    <automated>cd /Users/luana/Documents/Code\ Projects/MaosFalam && npm run build</automated>
  </verify>
  <done>
    - /ler/resultado/[id]?paid=1 refetches reading, redirects to /completo if premium, retries once if webhook delayed
    - /conta/leituras?purchased=1 shows success toast and cleans URL
    - Build passes with zero errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify full payment flow end-to-end on staging</name>
  <files>none</files>
  <action>
    Human verifies the complete payment flow on staging:
    1. Go to staging.maosfalam.com/creditos
    2. Verify: tarot deck works, NO PIX QR section, NO card form, NO payment method toggle
    3. Select a pack, click "Pagar" — should show processing then redirect to AbacatePay (or show 401 error if not logged in)
    4. If logged in without CPF: verify CPF field appears in payment section
    5. Go to a free reading result page — click "Desbloquear tudo" without credits — should redirect to /creditos?reading={id}
    6. Visit /conta/leituras?purchased=1 — should see gold toast "Seus creditos estao esperando."
  </action>
  <verify>Human visual inspection on staging</verify>
  <done>All 6 verification steps pass. Payment flow works end-to-end.</done>
</task>

</tasks>

<verification>
1. `npm run type-check` — zero type errors
2. `npm run build` — zero build errors
3. `npx vitest run -x` — full test suite passes (including new tests from 13-01)
4. Manual: UpsellSection click with no credits redirects to /creditos?reading={id}
5. Manual: /conta/leituras?purchased=1 shows toast
</verification>

<success_criteria>

- UpsellSection works: with credits -> upgrade, without credits -> /creditos?reading={id} (FRONT-04)
- ?paid=1 on resultado page refetches and redirects to /completo when premium (FRONT-05)
- ?purchased=1 on leituras page shows success toast (FRONT-05)
- No window.location.pathname parsing in UpsellSection
  </success_criteria>

<output>
After completion, create `.planning/phases/13-frontend-payment-flow/13-02-SUMMARY.md`
</output>
